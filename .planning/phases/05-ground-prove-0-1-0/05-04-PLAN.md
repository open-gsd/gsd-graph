---
phase: 05-ground-prove-0-1-0
plan: 04
type: execute
wave: 4
depends_on:
  - "05-01"
  - "05-02"
  - "05-03"
files_modified:
  - tests/golden-scenarios.test.ts
  - CHANGELOG.md
  - README.md
autonomous: true
requirements:
  - GOLD-01
  - GOLD-02
  - GOLD-03
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "G0: isolated free-prose.md corpus offline → pack/answer abstains or has no typed multi-hop path (causes|supports|contradicts|precedes|depends_on) (GOLD-01, D-07)"
    - "G1: isolated multi-hop.jsonl → paths ≥1 with ≥3 nodes and causes; citations include causes; answer not abstained (GOLD-02, D-08)"
    - "G2 cheap: path between drought and food-shortage non-empty typed on multi-hop store (discretion)"
    - "GOLD-03: package.json version remains 0.1.0; CHANGELOG has [0.1.0]; full npm test green including maintain M1–M5 and core CLI (GOLD-03, D-09)"
    - "Goldens use node:test and existing corpus fixtures (no network, no API keys) (D-05, D-12)"
  artifacts:
    - path: tests/golden-scenarios.test.ts
      provides: "G0 / G1 / cheap G2 honesty gates"
    - path: CHANGELOG.md
      provides: "Keep a Changelog 0.1.0 release notes (D-09)"
    - path: README.md
      provides: "CLI documents pack/answer (release readiness)"
  key_links:
    - from: G0 tmp corpus
      to: free-prose.md only
      via: "must not mix multi-hop.jsonl (pitfall 6, D-07)"
    - from: G1 tmp corpus
      to: multi-hop.jsonl only
      via: "isolated build then packSubgraph/answer (D-08)"
    - from: GOLD-03 gate
      to: npm test
      via: "M1–M5 + cli + pack-answer + golden-scenarios green (D-09)"
---

<objective>
Prove offline multi-hop honesty with G0/G1(+cheap G2) goldens and mark 0.1.0 release-ready via CHANGELOG + full green suite (GOLD-01, GOLD-02, GOLD-03, D-07, D-08, D-09, D-12).

Purpose: 0.1.0 ships only when free prose refuses to invent multi-hop edges and structured JSONL yields cited paths.
Output: `tests/golden-scenarios.test.ts`, `CHANGELOG.md`, README CLI touch-up; suite green gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ground-prove-0-1-0/CONTEXT.md
@.planning/phases/05-ground-prove-0-1-0/RESEARCH.md
@.planning/phases/05-ground-prove-0-1-0/05-01-SUMMARY.md
@.planning/phases/05-ground-prove-0-1-0/05-02-SUMMARY.md
@.planning/phases/05-ground-prove-0-1-0/05-03-SUMMARY.md
@src/pipeline/pack.ts
@src/pipeline/answer.ts
@tests/fixtures/corpus/free-prose.md
@tests/fixtures/corpus/multi-hop.jsonl
@tests/maintain.test.ts
@package.json
@README.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Golden G0 free-prose abstain + G1 multi-hop path</name>
  <files>tests/golden-scenarios.test.ts</files>
  <behavior>
    - G0 (GOLD-01, D-07): mkdtemp store + corpus containing only free-prose.md; init + build({ full: true }); packSubgraph/answer for a multi-hop style question (e.g. why does drought cause food shortage?) → either abstained true OR no path whose predicates include typed multi-hop set causes|supports|contradicts|precedes|depends_on. Do not require zero nodes/triples (about edges allowed).
    - G1 (GOLD-02, D-08): isolated multi-hop.jsonl build; pack paths.length ≥ 1; some path nodes.length ≥ 3 and predicates includes causes; citations some p === causes; answer.abstained === false and mode deterministic; markdown matches causes.
    - Fixtures never mixed in the same G0 corpus dir (pitfall 6)
    - Offline only — no env API keys, no network (D-05, D-12)
  </behavior>
  <action>
    Create tests/golden-scenarios.test.ts with isolated corpus builds (per D-07, D-08, D-12, RESEARCH Pattern 4).

    1. Harness: mkdtemp store root + corpus dir; copy single fixture file into corpus; init({ dir, cwd }); build({ corpus, dir, full: true }); require packSubgraph/answer/nodeId from dist/index.js (same pattern as query.test.ts).

    2. G0: copy only tests/fixtures/corpus/free-prose.md. Question fixed string for drought/food multi-hop. Assert honesty per D-07 wording (abstain OR no typed multi-hop path). Prefer also asserting answer.mode is abstain when pack has no typed multi-hop triples for that question.

    3. G1: copy only multi-hop.jsonl. Use nodeId('Concept','Drought') / Food Shortage for optional path cross-check. Assert path and citation gates from RESEARCH G1 skeleton.

    4. Optional thin question sidecars under tests/fixtures/golden/ only if useful — do not duplicate corpus content (D-12).

    5. Clean temps in afterEach.
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern='golden|G0|G1'</automated>
  </verify>
  <done>G0 free-prose honesty and G1 multi-hop path goldens pass offline (GOLD-01, GOLD-02, D-07, D-08).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cheap G2 + CHANGELOG 0.1.0 + release suite gate</name>
  <files>tests/golden-scenarios.test.ts, CHANGELOG.md, README.md</files>
  <behavior>
    - G2 cheap (discretion): on multi-hop store, query({ path: { from: drought, to: foodShortage } }) or pack paths non-empty typed causes chain (GOLD-02 family)
    - G3/G4: document in golden test comments or CHANGELOG notes as covered by query applyBudget tests and maintain M1–M5 — no full reimplementation required (D-09 discretion)
    - CHANGELOG.md Keep a Changelog style with ## [0.1.0] section covering pack/answer, goldens G0/G1, query/CLI foundation (D-09, GOLD-03)
    - package.json version stays 0.1.0 — do not bump (D-09)
    - README CLI section lists pack and answer (and existing verbs) so docs match binary (release readiness)
    - Full npm test green: maintain, query, cli, pack-answer, golden-scenarios (GOLD-03, D-09)
  </behavior>
  <action>
    Close GOLD-03 release readiness (per D-09, D-05, D-12).

    1. Add G2 assert in golden-scenarios (reuse G1 store or separate multi-hop build): path non-empty with causes.

    2. Create CHANGELOG.md at repo root (if missing) with # Changelog and ## [0.1.0] - YYYY-MM-DD (release-day or planning date is fine). Sections: Added (packSubgraph, answer, CLI pack/answer, offline goldens G0/G1, prior milestone bullets as needed); Notes (LLM/MCP deferred Phase 6; communities 0.2.0).

    3. Update README.md CLI / usage text that still says pack/answer ship later — document pack and answer with brief examples consistent with K22 JSON stdout.

    4. Run full npm test (and preferably npm run test:coverage if already gated in CI). Fix any residual failures from this phase. Confirm package.json version is 0.1.0.

    5. Do not publish npm or create git tag in-plan unless user asks — gate is green suite + docs (GOLD-03).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>G2 cheap covered; CHANGELOG 0.1.0 present; full suite green; version 0.1.0 releasable (GOLD-03, D-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Golden fixtures → extract/pack | Offline honesty bar; no network LLM |
| CHANGELOG / README | Public claim surface for 0.1.0 capabilities |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11 | Spoofing | G0 free-prose | high | mitigate | Isolated corpus; assert no typed multi-hop path / abstain (GOLD-01, D-07) |
| T-05-12 | Spoofing | G1 citations | high | mitigate | Path + causes + citations ⊆ triples (GOLD-02, D-08) |
| T-05-13 | Tampering | release notes overclaim | medium | mitigate | CHANGELOG only lists shipped surface; LLM/MCP noted deferred (D-05, D-09) |
| T-05-SC | Tampering | npm installs | high | mitigate | No new packages for goldens/docs |
</threat_model>

<verification>
- node --test golden-scenarios green via npm test
- Full npm test green (GOLD-03)
- CHANGELOG.md and README reflect pack/answer
</verification>

<success_criteria>
- GOLD-01, GOLD-02, GOLD-03 satisfied
- Phase 5 roadmap success criteria 3–5 true
- Package releasable as 0.1.0 when suite green
</success_criteria>

<output>
Create `.planning/phases/05-ground-prove-0-1-0/05-04-SUMMARY.md` when done
</output>
