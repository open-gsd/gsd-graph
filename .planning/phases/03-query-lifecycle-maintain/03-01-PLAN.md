---
phase: 03-query-lifecycle-maintain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/ids.ts
  - src/pipeline/query.ts
  - src/types.ts
  - src/index.ts
  - tests/query.test.ts
autonomous: true
requirements:
  - QRY-01
  - QRY-02
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "query({ path: { from, to, maxDepth } }) returns a node chain with predicates for a multi-hop EXTRACTED path (QRY-01, D-01, D-03)"
    - "query({ term, hops }) seed-expands from id/label/alias substring matches (QRY-01, D-01)"
    - "query({ id, hops }) neighborhood expands undirected hops from a node id (QRY-01)"
    - "query filter by types/predicates/confidenceMin uses the same tier ranks as bestTier (QRY-02, D-02)"
    - "applyBudget uses ceil(JSON.stringify(subgraph).length/4) and drops AMBIGUOUS before INFERRED before EXTRACTED (QRY-02, D-02)"
    - "query loads only graph.v1 via loadGraphV1 (or in-memory opts.graph) — never projection as SoT (D-04)"
  artifacts:
    - path: src/pipeline/query.ts
      provides: "query IR dispatcher, adjacency BFS, applyBudget, seedAndExpand, buildAdjacencyMap"
    - path: src/pipeline/ids.ts
      provides: "exported confidenceRank shared with bestTier ranks"
    - path: tests/query.test.ts
      provides: "QRY-01 path/seed/neighborhood/filter + QRY-02 budget/rank gates"
  key_links:
    - from: confidenceRank
      to: bestTier TIER_RANK
      via: "single rank table EXTRACTED=2 INFERRED=1 AMBIGUOUS=0 (D-02)"
    - from: query
      to: loadGraphV1
      via: "disk path when opts.graph absent (D-04)"
    - from: applyBudget
      to: QueryResult.trimmed
      via: "token estimate ceil(JSON/4); drop worst first"
---

<objective>
Ship pure-TS Query IR (term seed-expand, path, neighborhood, filter) with shared confidence ranks and DESIGN budget filtering.

Purpose: QRY-01/QRY-02 multi-hop read path over published graph.v1 without graphology/ngraph or NL→IR (D-01, D-03).
Output: `src/pipeline/query.ts`, `confidenceRank` export, Query types, `tests/query.test.ts`, public façade exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-query-lifecycle-maintain/CONTEXT.md
@.planning/phases/03-query-lifecycle-maintain/RESEARCH.md
@.planning/phases/02-build-pipeline/02-04-SUMMARY.md
@docs/DESIGN.md
@src/pipeline/ids.ts
@src/io/load-graph.ts
@src/types.ts
@src/index.ts
@src/errors.ts
@tests/fixtures/corpus/multi-hop.jsonl
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end path query on multi-hop graph</name>
  <files>src/pipeline/ids.ts, src/pipeline/query.ts, src/types.ts, src/index.ts, tests/query.test.ts</files>
  <behavior>
    - confidenceRank('EXTRACTED') === 2, confidenceRank('INFERRED') === 1, confidenceRank('AMBIGUOUS') === 0; same order as bestTier (D-02, QRY-02)
    - buildAdjacencyMap(graph) indexes undirected edges for traversal while preserving directed triple predicates
    - query({ graph, path: { from, to, maxDepth: 6 } }) on Drought→Crop Failure→Food Shortage chain returns paths[0] with ≥3 node ids and predicates including causes (QRY-01, D-01, D-03)
    - query with only opts.graph never calls loadGraphV1 / never opens graph.json (D-04)
    - missing op fields (no term/path/id/filter) throws GraphError with a clear usage message
  </behavior>
  <action>
    Implement the thinnest vertical Query IR slice: shared ranks + pure-TS path BFS + public query entry (per D-01 structured-only, D-02 ranks, D-03 pure-TS, D-04 v1-only, D-11 headers, D-12 node:test).

    1. Export confidenceRank(c: Confidence): number from src/pipeline/ids.ts by reusing the existing TIER_RANK table used by bestTier (do not duplicate a second rank map — D-02). Keep bestTier behavior unchanged.

    2. Add types in src/types.ts (discretion shapes OK if ops match DESIGN QueryIR):
       - QueryIR discriminated union: seed_expand | path | neighborhood | filter
       - QueryOptions bag: dir?, term?, hops?, budget?, path?: { from, to, maxDepth? }, id?, types?, predicates?, confidenceMin?, graph?: GraphV1Document
       - QueryResult: nodes, triples, paths: Array&lt;{ nodes: string[]; predicates: string[] }&gt;, seeds: string[], trimmed: string | null, budget_tokens: number | null

    3. Create src/pipeline/query.ts (D-11 copyright header):
       - buildAdjacencyMap(graph): Map from node id to neighbor edges { neighbor, predicate, tripleId, directed s→o }
       - findShortestPath undirected BFS (OQ-4 RESOLVED): treat edges bidirectional for walk; return actual predicates on the directed triples used; fewer edges wins; tie-break lexicographically smaller predicate then neighbor id; clamp maxDepth (default 6, hard clamp ≤16)
       - materialize path → nodes + triples from graph
       - query(opts): if opts.graph use it; else loadGraphV1(resolveStoreRoot({ dir: opts.dir })) — never read projection (D-04)
       - For this tracer, implement at least the path branch; other branches may throw "not implemented" only if Task 2 immediately lands in same plan wave — prefer implementing path fully green first

    4. tests/query.test.ts (D-12): require dist/index.js like existing suite. Build in-memory GraphV1Document with Concept nodes Drought / Crop Failure / Food Shortage and EXTRACTED causes triples (ids via tripleId). Assert path from Drought id to Food Shortage id yields ≥3 nodes and causes predicates. Assert confidenceRank order. Use opts.graph so no temp store required for tracer.

    5. Export confidenceRank, query, buildAdjacencyMap, and Query* types from src/index.ts.
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern='query|path|confidenceRank'</automated>
  </verify>
  <done>Path query returns multi-hop chain with predicates; confidenceRank matches bestTier ranks; pure-TS only (D-01, D-02, D-03, D-04).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: seed_expand, neighborhood, and filter IR ops</name>
  <files>src/pipeline/query.ts, tests/query.test.ts</files>
  <behavior>
    - query({ graph, term: 'drought', hops: 2 }) seeds on id/label/alias case-folded substring match and expands undirected hops; returns seeds non-empty and triples along expansion (QRY-01, D-01)
    - query({ graph, id: droughtNodeId, hops: 1 }) neighborhood returns only nodes/triples within 1 hop (QRY-01)
    - query({ graph, predicates: ['causes'], confidenceMin: 'EXTRACTED' }) filter keeps EXTRACTED causes triples and endpoint nodes; confidenceMin uses confidenceRank ≥ threshold (QRY-01, QRY-02, D-02)
    - query({ graph, types: ['Concept'] }) includes Concept nodes that appear as endpoints or typed matches per filter design
    - hops default: seed_expand 2, neighborhood 1; hops clamped ≤16
  </behavior>
  <action>
    Expand query.ts with remaining structured ops (D-01, D-03 undirected expansion, D-12 tests). No NL→IR. No graphology/ngraph.

    1. matchTermSeeds(graph, term): case-fold substring against node id, label, and aliases; return Set of seed ids.

    2. expandHops(adj, graph, seeds, hops): BFS undirected up to hops; collect reachable nodes + incident triples; return { nodes, triples }.

    3. filterGraph(graph, opts): filter triples by predicates allowlist if provided; by confidenceRank(t.confidence) ≥ confidenceRank(confidenceMin) if provided; collect endpoint nodes; if types provided, restrict nodes to those types (and drop triples whose endpoints are not both kept unless product chooses endpoint-open filter — prefer: type filter applies to returned nodes; triples kept only when both s and o remain in node set after type filter, or when types omitted).

    4. query() dispatch order (document in module header): path if opts.path; else neighborhood if opts.id; else filter if types|predicates|confidenceMin without term; else seed_expand if term !== undefined; else throw usage GraphError. When term is set with filter fields, term seed_expand wins (filter fields ignored) unless you document combined semantics — prefer simple exclusive dispatch.

    5. Export seedAndExpand (or expandHops under that name) from query.ts and index for later pack composition (export helper only — do not implement packSubgraph; Phase 5 deferred).

    6. Extend tests/query.test.ts with seed, neighborhood, filter, confidenceMin rank consistency cases (D-12).
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern='query|seed|neighborhood|filter'</automated>
  </verify>
  <done>All four Query IR ops work on in-memory graphs with undirected expansion and shared ranks (QRY-01, QRY-02, D-01, D-02).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: applyBudget token trim + disk loadGraphV1 path</name>
  <files>src/pipeline/query.ts, src/index.ts, tests/query.test.ts</files>
  <behavior>
    - applyBudget drops AMBIGUOUS before INFERRED before EXTRACTED; tie-break triple.id ascending (QRY-02, D-02, RESEARCH OQ-2)
    - budget unit is Math.ceil(JSON.stringify({ nodes, triples }).length / 4); budget null/undefined skips trim (RESEARCH OQ-2 RESOLVED)
    - seed nodes retained when possible while dropping non-seed triples
    - query({ dir, term }) loads graph.v1 via loadGraphV1 after a real build into temp store; still never reads graph.json (D-04)
    - QueryResult.budget_tokens and trimmed reflect trim activity
  </behavior>
  <action>
    Complete budget filtering and disk read path (D-02, D-04, D-12).

    1. applyBudget(nodes, triples, budgetTokens, seedIds): if budgetTokens == null or ≤0 return unchanged with trimmed null. Sort drop candidates by confidenceRank ascending then id ascending. While estimate exceeds budget, drop first candidate. Rebuild node set from seeds ∪ triple endpoints. Set trimmed to a short description of last drop (or cumulative note). Export applyBudget.

    2. Wire applyBudget at end of every query() branch before return.

    3. Disk integration test: build({ corpus: multi-hop.jsonl fixture, dir: tempStore }) then query({ dir: tempStore, path or term }) asserting multi-hop structure; assert graph.json absence or ignore — query must succeed without projection (D-04).

    4. Budget unit test: craft triples with AMBIGUOUS + EXTRACTED; set tiny budget so at least one drop occurs; assert AMBIGUOUS dropped while EXTRACTED path edge retained when possible (PITFALLS budget order).

    5. Confirm public exports: query, applyBudget, buildAdjacencyMap, seedAndExpand (or documented name), confidenceRank, QueryOptions, QueryResult, QueryIR.

    6. No new npm packages (D-03). Copyright headers on all new files (D-11).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Budget uses DESIGN token estimate and shared ranks; disk query reads only graph.v1; QRY-01 and QRY-02 green (D-02, D-04, D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → query(opts) | Untrusted term/path/id/filter fields and optional dir |
| query → store graph.v1 | Read-only SoT; projection must not be loaded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Denial of service | query hops/maxDepth | medium | mitigate | Clamp hops/maxDepth ≤16; reuse existing graph hard caps |
| T-03-02 | Tampering | query SoT confusion | high | mitigate | loadGraphV1 only; opts.graph for tests; never open graph.json (D-04) |
| T-03-03 | Information disclosure | query term matching | low | accept | Local offline library; no multi-tenant isolation |
| T-03-SC | Tampering | npm installs | high | mitigate | No new packages; ban graphology/ngraph (D-03) |
</threat_model>

<verification>
- npm test green including tests/query.test.ts
- Path multi-hop + seed + neighborhood + filter + budget order assertions
- No graphology/ngraph in package.json dependencies
</verification>

<success_criteria>
QRY-01 and QRY-02 observable: structured Query IR ops work; budget/filter ranks match bestTier; pure-TS adjacency only (D-01..D-04, D-12).
</success_criteria>

<output>
Create `.planning/phases/03-query-lifecycle-maintain/03-01-SUMMARY.md` when done
</output>
