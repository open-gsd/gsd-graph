---
phase: 03-query-lifecycle-maintain
plan: 04
type: execute
wave: 4
depends_on:
  - "03-02"
  - "03-03"
files_modified:
  - src/pipeline/diff.ts
  - src/pipeline/repair.ts
  - src/pipeline/project.ts
  - src/types.ts
  - src/index.ts
  - tests/diff.test.ts
  - tests/repair.test.ts
autonomous: true
requirements:
  - DIFF-01
  - REP-01
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "diff compares current graph.v1 to named snapshot or snapshots/.last-diff-base.json by node/triple id (± added/removed/changed) (DIFF-01, D-08)"
    - "diff with no snapshot and no last-diff-base throws NO_BASELINE (DIFF-01, D-08)"
    - "repair loads graph.v1 only, regenerates graph.json via projectGraph, invents no triples (REP-01, D-09)"
    - "repair holds build lock during publish (D-10)"
  artifacts:
    - path: src/pipeline/diff.ts
      provides: "diff() DiffResult vs snapshot or last-diff-base"
    - path: src/pipeline/repair.ts
      provides: "repair() projection regenerate from v1"
    - path: tests/diff.test.ts
      provides: "DIFF-01 ± id arithmetic + NO_BASELINE"
    - path: tests/repair.test.ts
      provides: "REP-01 projection-from-v1 only"
  key_links:
    - from: diff baseline resolution
      to: named snapshot then .last-diff-base
      via: "DESIGN order; else NO_BASELINE (D-08)"
    - from: repair
      to: loadGraphV1 + projectGraph + publishGraphFiles
      via: "v1 sole input; projection disposable (D-09)"
---

<objective>
Ship diff (current vs snapshot / last-diff-base by id) and repair (regenerate projection from graph.v1 only).

Purpose: DIFF-01 and REP-01 complete Phase 3 lifecycle correctness and operator recovery without elevating projection to SoT.
Output: diff.ts, repair.ts, types, tests, public exports; Phase 3 library façade complete.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-query-lifecycle-maintain/CONTEXT.md
@.planning/phases/03-query-lifecycle-maintain/RESEARCH.md
@docs/DESIGN.md
@src/pipeline/project.ts
@src/pipeline/snapshot.ts
@src/pipeline/build.ts
@src/io/load-graph.ts
@src/io/atomic-publish.ts
@src/io/lock.ts
@src/errors.ts
@src/types.ts
@src/index.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end diff vs last-diff-base and named snapshot</name>
  <files>src/pipeline/diff.ts, src/types.ts, src/index.ts, tests/diff.test.ts</files>
  <behavior>
    - After build A (writes last-diff-base), mutate graph via second build or controlled publish, diff({ dir }) reports triples.removed and/or triples.added by id with counts matching arrays (DIFF-01, D-08, K25)
    - diff({ dir, snapshot: 'name' }) uses named snapshot baseline when provided (resolution order: snapshot arg → last-diff-base → NO_BASELINE)
    - Fresh empty store / store without baseline throws GraphError reason NO_BASELINE (D-08)
    - Node/triple changed = same id, different stable payload excluding volatile store-level timestamps (DESIGN); at minimum detect label/confidence/provenance payload differences
    - diff reads only graph.v1 for current graph (D-04)
  </behavior>
  <action>
    Implement pure id-set DiffResult (per D-08, D-04, D-11, D-12; DESIGN DiffResult; RESEARCH OQ-3 baseline).

    1. Types — DiffOptions { dir?: string; snapshot?: string }, DiffResult exactly:
       - baseline: string
       - nodes: { added: string[]; removed: string[]; changed: string[] }
       - triples: { added: string[]; removed: string[]; changed: string[] }
       - counts: { nodes_added, nodes_removed, triples_added, triples_removed } (numbers; changed may be omitted from counts per DESIGN — follow DESIGN fields as written)

    2. src/pipeline/diff.ts:
       - resolveBaseline(storeRoot, snapshot?): if snapshot provided, resolve under snapshots/ (reuse snapshot name resolution from snapshot.ts or shared helper — do not duplicate unsafe path join); else if snapshots/.last-diff-base.json exists use it; else throw GraphError(GSD_GRAPH_REASON.NO_BASELINE, ...)
       - Load current via loadGraphV1; load baseline JSON + validateGraphV1
       - Set arithmetic by id for nodes and triples; changed when id in both but comparable payload differs (compare type/label/aliases for nodes; s/p/o/confidence/provenance for triples; exclude built_at engine fields at document level)

    3. tests/diff.test.ts: temp store build → capture triple ids → change corpus / rebuild → diff default baseline; named snapshot path; NO_BASELINE on empty store.

    4. Export diff + DiffResult types from index.ts.
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern='diff|NO_BASELINE|baseline'</automated>
  </verify>
  <done>DIFF-01 green: ± by id vs snapshot or last-diff-base; NO_BASELINE when missing (D-08).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: repair projection from v1 only + façade completeness</name>
  <files>src/pipeline/repair.ts, src/pipeline/project.ts, src/types.ts, src/index.ts, tests/repair.test.ts</files>
  <behavior>
    - Given store with graph.v1 and no graph.json, repair({ dir }) creates graph.json whose edges.length === triple_count and every edge id ∈ v1 triple ids (REP-01, D-09)
    - repair does not add triples or nodes beyond v1; deleting graph.json and repairing restores projection only (REP-01)
    - Missing graph.v1 → SCHEMA_INVALID (same as loadGraphV1); never invents a graph from projection (D-04, D-09)
    - acquireBuildLock held during repair publish and released after (D-10)
    - Public index exports include query, maintain, invalidateProvenance, projectGraph, snapshot*, diff, repair for Phase 4 CLI (library only this phase)
  </behavior>
  <action>
    Implement repair and close Phase 3 public surface (per D-09, D-10, D-11, D-12, REP-01).

    1. src/pipeline/repair.ts — repair(opts?: { dir?: string; writeProjection?: boolean }):
       - resolve/ensure store root
       - acquireBuildLock(storeRoot, 'lib')
       - loadGraphV1(storeRoot) — sole input
       - projection = projectGraph(graphV1)
       - publishGraphFiles({ storeRoot, graphV1, projection, writeProjection: true }) so graph.json materializes (repair's job)
       - return RepairResult: store_dir, node_count, triple_count, projection_written: true, reason: OK
       - finally release lock

    2. Reuse projectGraph from src/pipeline/project.ts (created in 03-02); if any field mismatch with DESIGN edge shape, fix once in project.ts.

    3. tests/repair.test.ts:
       - build with writeProjection false → no graph.json → repair → graph.json edges match triples
       - assert edge ids ⊆ triple ids; no extra edges
       - optional: corrupt/remove projection only, repair restores from v1
       - missing v1 throws SCHEMA_INVALID

    4. Export repair + RepairResult; verify index.ts exports the full Phase 3 library set (query, applyBudget, maintain, invalidateProvenance, projectGraph, snapshotSave/List/Restore, diff, repair, confidenceRank).

    5. Run full suite; keep coverage ≥80 lines (D-12). No new packages.
  </action>
  <verify>
    <automated>npm run test:coverage</automated>
  </verify>
  <done>REP-01 and DIFF-01 complete; Phase 3 library APIs exported; coverage ≥80 (D-08, D-09, D-10, D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| diff baseline files → comparison | Baseline JSON must validate as graph.v1 |
| repair → graph.json | Projection output only; v1 remains SoT |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | Tampering | diff baseline path | high | mitigate | confine under snapshots/; validateGraphV1; NO_BASELINE if absent (D-08) |
| T-03-11 | Tampering | repair invents data | high | mitigate | loadGraphV1 only + projectGraph map; never read graph.json as input (D-09, REP-01) |
| T-03-12 | Tampering | concurrent repair | high | mitigate | acquireBuildLock (D-10) |
| T-03-SC | Tampering | npm installs | high | mitigate | No new packages |
</threat_model>

<verification>
- npm test + npm run test:coverage
- diff ± ids and NO_BASELINE
- repair writes projection from v1 only
</verification>

<success_criteria>
DIFF-01 and REP-01 true; Phase 3 ROADMAP success criteria 1–4 achievable with QRY/MNT/SNAP from prior plans.
</success_criteria>

<output>
Create `.planning/phases/03-query-lifecycle-maintain/03-04-SUMMARY.md` when done
</output>
