---
phase: 04-cli-surface
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - tests/cli.test.ts
  - tests/package-identity.test.ts
autonomous: true
requirements:
  - PKG-03
  - CLI-01
  - CLI-02
  - CLI-03
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "spawnSync(process.execPath, [bin, ...]) happy path init → build --corpus → query → path returns exit 0 with JSON.parse(stdout) each step without a TTY (D-12, CLI-02, D-11)"
    - "Usage / unknown command (including pack and answer) exits 1 (CLI-01 Phase 4 subset, CLI-02, D-02, D-03)"
    - "Operational GraphError (e.g. diff with no baseline, or query/status on empty store as applicable) exits 2 with stderr {ok:false,reason,message} (CLI-02, D-03, D-04)"
    - "build_locked surfaces as exit 3 with reason build_locked on stderr JSON (CLI-02, D-03)"
    - "Published bin remains invokable via package.json bin field and shebang file (PKG-03)"
  artifacts:
    - path: tests/cli.test.ts
      provides: "Process-level E2E happy path + exit code matrix via spawnSync"
  key_links:
    - from: spawnSync
      to: bin/gsd-graph.js
      via: "process.execPath + bin path; cwd=tmpdir; encoding utf8; NO_COLOR=1"
    - from: happy path
      to: tests/fixtures/corpus
      via: "build --corpus multi-hop.jsonl or structured fixture"
    - from: lock test
      to: .build.lock
      via: "plant lock file then gsd-graph build → exit 3"
---

<objective>
Prove the machine CLI contract end-to-end: process-spawn happy path and exit 0/1/2/3 matrix (PKG-03, CLI-01, CLI-02, CLI-03, D-12).

Purpose: Agents can trust JSON stdout and K22 exits without a TTY before Phase 5 pack/answer.
Output: `tests/cli.test.ts` spawn suite; package-identity bin confirmation if any residual gaps.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-cli-surface/CONTEXT.md
@.planning/phases/04-cli-surface/RESEARCH.md
@.planning/phases/04-cli-surface/04-01-SUMMARY.md
@.planning/phases/04-cli-surface/04-02-SUMMARY.md
@bin/gsd-graph.js
@src/cli.ts
@tests/fixtures/corpus/multi-hop.jsonl
@tests/package-identity.test.ts
@src/io/lock.ts
@src/errors.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SpawnSync happy path init → build → query → path</name>
  <files>tests/cli.test.ts</files>
  <behavior>
    - Helper run(args, cwd) = spawnSync(process.execPath, [binPath, ...args], { cwd, encoding: 'utf8', env: { ...process.env, NO_COLOR: '1' } }) (D-11)
    - Fresh tmp cwd with .gitignore present: init exit 0; stdout JSON has store_dir; .gsd-graph exists; .gitignore contains .gsd-graph/ (CLI-03, D-05, D-12)
    - build --corpus &lt;fixture&gt; exit 0; stdout JSON has node_count/triple_count or engine field (CLI-01, D-12)
    - query term from multi-hop fixture (e.g. drought) exit 0; stdout JSON parseable with nodes/triples keys (CLI-01, D-12)
    - path from→to node ids from fixture chain exit 0; stdout JSON parseable (CLI-01, D-12)
    - Each successful stdout is JSON-only (JSON.parse full stdout trim) — no leading log lines (CLI-02, D-03)
  </behavior>
  <action>
    Create tests/cli.test.ts process E2E for D-12 (per D-03, D-05, D-11, D-12).

    1. Resolve bin = path.join(repoRoot, 'bin', 'gsd-graph.js'); require dist built via npm test pipeline.

    2. Use fs.mkdtempSync under os.tmpdir(); write a minimal .gitignore ("node_modules\n"); copy or point --corpus at absolute path to tests/fixtures/corpus/multi-hop.jsonl (and/or parent corpus dir if build expects a directory — match library build() corpus root behavior from status/build tests: pass directory containing the jsonl, not necessarily a single file — check existing tests/build-pipeline or status tests for corpus path shape and mirror it).

    3. Sequence: init → build --corpus … → query &lt;term&gt; → path &lt;fromId&gt; &lt;toId&gt;. Discover from/to ids from build/query JSON if needed (parse query seeds/nodes) rather than hardcoding fragile ids — or use known nodeId() scheme from fixture labels if stable.

    4. Assert status 0 and JSON.parse each stdout.

    5. Copyright header D-10 on test file.
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern='cli happy|happy path|init → build'</automated>
  </verify>
  <done>Process-level happy path init→build→query→path exits 0 with structured JSON (D-12, CLI-02, CLI-03).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exit code matrix 1/2/3 + pack/answer unknown + bin identity</name>
  <files>tests/cli.test.ts, tests/package-identity.test.ts</files>
  <behavior>
    - Unknown command and unregistered pack/answer exit status 1 (CLI-01 subset, D-02, D-03)
    - Missing required build --corpus (or equivalent usage) exits 1 (CLI-02)
    - Operational failure exits 2 with stderr JSON ok:false and reason string (e.g. diff without baseline after init-only store, or corpus_not_found for bad --corpus) (CLI-02, D-03, D-04)
    - Plant .build.lock under store (valid lock payload shape matching acquireBuildLock expectations if required) then build → exit 3; stderr reason build_locked (CLI-02, D-03)
    - package-identity still asserts bin publish fields (PKG-03)
  </behavior>
  <action>
    Complete CLI-02 machine contract proof at process boundary (per D-02, D-03, D-04, D-11).

    1. Exit 1 cases via spawnSync: `pack`, `answer`, totally-unknown-verb, `build` without --corpus.

    2. Exit 2 case: after init only (no build), run `diff` expecting NO_BASELINE → status 2; parse stderr JSON { ok: false, reason: 'no_baseline', message }; stdout empty or non-success. Alternative: build --corpus /nonexistent → corpus_not_found exit 2.

    3. Exit 3 case: init + write store .build.lock using the same JSON shape the lock module writes (read src/io/lock.ts for fields: pid, started_at, owner, etc.); use current process.pid so lock is non-stale; run build --corpus fixture; expect status 3 and stderr reason build_locked. Clean up lock in afterEach.

    4. Confirm stderr error bodies match D-04 shape for exit 2 and 3.

    5. If package-identity bin gates missing anything from 04-01, close residual gaps only (do not expand scope).
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern='cli|exit|build_locked|pack|answer|package identity'</automated>
  </verify>
  <done>Full K22 exit matrix and PKG-03 bin proven via process tests (CLI-01, CLI-02, PKG-03, D-02, D-03, D-04, D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → CLI process | Spawned process with crafted argv and cwd |
| CLI process → temp filesystem | Isolated tmpdir stores for lock/gitignore cases |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Tampering | stdout contract under spawn | medium | mitigate | Assert JSON.parse(full stdout) — fails if logs leak |
| T-04-09 | Denial of service | lock held during build | medium | mitigate | Assert exit 3 distinguishes lock from other errors |
| T-04-SC | Tampering | dependencies | low | accept | No new installs in this plan; prior pins hold |
</threat_model>

<verification>
- `npm test` full suite green
- Happy path and exit matrix tests pass under pipes (non-TTY)
- ROADMAP Phase 4 success criteria 1–4 demonstrably true via tests
</verification>

<success_criteria>
- D-12 happy path automated at process level
- Exit codes 0/1/2/3 covered with D-04 stderr shape for failures
- pack/answer unknown exit 1
- PKG-03 bin still enforced
</success_criteria>

<output>
Create `.planning/phases/04-cli-surface/04-03-SUMMARY.md` when done
</output>
