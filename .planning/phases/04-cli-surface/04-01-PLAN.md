---
phase: 04-cli-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - bin/gsd-graph.js
  - src/cli.ts
  - src/pipeline/init.ts
  - src/types.ts
  - src/index.ts
  - tests/init.test.ts
  - tests/package-identity.test.ts
autonomous: true
requirements:
  - PKG-03
  - CLI-02
  - CLI-03
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "package.json publishes bin gsd-graph → ./bin/gsd-graph.js and files includes bin (PKG-03, D-01)"
    - "bin/gsd-graph.js has a node shebang and always invokes exported main(process.argv) from dist/cli.js (PKG-03, D-01)"
    - "main(argv) returns 0 and writes JSON-only stdout for init success (CLI-02, D-03, D-04)"
    - "GraphError maps to exit 2 with stderr {ok:false,reason,message}; build_locked maps to exit 3 (CLI-02, D-03, D-04)"
    - "init creates store layout under .gsd-graph or --dir and appends the store entry to an existing .gitignore without creating one (CLI-03, D-05)"
    - "init is idempotent: second run does not duplicate the gitignore line (CLI-03, D-05)"
  artifacts:
    - path: bin/gsd-graph.js
      provides: "npm bin shebang wrapper that requires dist/cli.js and calls main(process.argv)"
    - path: src/cli.ts
      provides: "commander program, main(argv)→exitCode, K22 I/O helpers, init command"
    - path: src/pipeline/init.ts
      provides: "library init() store layout + gitignore append"
    - path: tests/init.test.ts
      provides: "CLI-03 layout/gitignore + K22 exit mapping for init path"
  key_links:
    - from: bin/gsd-graph.js
      to: dist/cli.js main
      via: "const { main } = require('../dist/cli.js'); process.exitCode = main(process.argv) — do not rely on require.main alone (PKG-03)"
    - from: cli init action
      to: init()
      via: "thin adapter passes --dir/--ontology; no pipeline reimplementation (D-06)"
    - from: main catch
      to: GraphError / CommanderError
      via: "exit 3 iff reason===build_locked else 2; usage→1 (D-03)"
---

<objective>
Ship the publishable `gsd-graph` binary skeleton with K22 exit mapping and a working library+CLI `init` path (PKG-03, CLI-02 foundation, CLI-03).

Purpose: Prove bin → cli → library → store end-to-end before wiring the remaining commands (D-01, D-03, D-05, D-06, D-09).
Output: `bin/gsd-graph.js`, `src/cli.ts` with `main()`, `src/pipeline/init.ts`, package `bin`/`files`/`dependencies`, `tests/init.test.ts`, package-identity bin gates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-cli-surface/CONTEXT.md
@.planning/phases/04-cli-surface/RESEARCH.md
@docs/DESIGN.md
@package.json
@src/index.ts
@src/errors.ts
@src/io/paths.ts
@src/pipeline/review.ts
@tests/package-identity.test.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end init via bin — deps, package bin, CLI skeleton, library init</name>
  <files>package.json, package-lock.json, bin/gsd-graph.js, src/cli.ts, src/pipeline/init.ts, src/types.ts, src/index.ts, tests/init.test.ts</files>
  <behavior>
    - package.json has bin.gsd-graph = "./bin/gsd-graph.js"; files array includes "bin"; dependencies include commander ^14.0.3 and picocolors ^1.1.1 (not commander 15) (PKG-03, D-01, D-09)
    - bin/gsd-graph.js starts with #!/usr/bin/env node and sets process.exitCode = main(process.argv) after requiring dist/cli.js (D-01, PKG-03)
    - init({ dir?, ontology?, cwd? }) creates store root via resolveStoreRoot + ensureStoreRoot, writes minimal config.json when missing, ensures snapshots/ directory, returns JSON-serializable { store_dir, created, gitignore_appended, ontology } (CLI-03, D-05)
    - When cwd has .gitignore and entry missing, init appends relative store dir with trailing slash (default .gsd-graph/); when .gitignore absent, does not create it; gitignore_appended reflects the write (CLI-03, D-05)
    - main(['node','gsd-graph','init','--dir', tmpStore]) returns 0; stdout is one JSON object parseable; GraphError path writes stderr D-04 shape and returns 2 (CLI-02, D-03, D-04)
  </behavior>
  <action>
    Ship the thinnest vertical CLI slice: publishable bin → commander adapter → library init (per D-01, D-03, D-04, D-05, D-06, D-07, D-09, D-10, D-11).

    1. Install deps exactly: `npm install commander@^14.0.3 picocolors@^1.1.1` (RESEARCH Package Legitimacy OK; do not install commander 15 — engines mismatch with package engines &gt;=22.0.0).

    2. Update package.json (PKG-03, D-01):
       - Add `"bin": { "gsd-graph": "./bin/gsd-graph.js" }`
       - Ensure `"files"` includes `"bin"` alongside existing dist/schemas/ontology-packs/LICENSE/README.md
       - Leave engines.node as &gt;=22.0.0

    3. Create bin/gsd-graph.js (D-10 header comment style as JS block or // lines). MUST invoke main — when npm runs the bin file, require.main is the bin path, not dist/cli.js, so a bare require does not enter a require.main === module guard:
       - First line shebang `#!/usr/bin/env node`
       - Body (exact contract):
         'use strict';
         const { main } = require('../dist/cli.js');
         process.exitCode = main(process.argv);
       - Do not use only require('../dist/cli.js') without calling main

    4. Add InitOptions / InitResult types in src/types.ts:
       - InitOptions: dir?, ontology?, cwd?
       - InitResult: store_dir: string; created: boolean; gitignore_appended: boolean; ontology: string

    5. Implement src/pipeline/init.ts (D-05, CLI-03, D-10 header):
       - resolveStoreRoot({ dir, cwd }) then ensureStoreRoot
       - Write minimal config.json under store via storeFile(root, 'config.json') only if missing — fields at minimum: ontology pack id/path (default general), optional store.write_projection following DEFAULT_WRITE_PROJECTION import or literal false
       - mkdir snapshots under store (recursive) if missing
       - Gitignore algorithm (only when path.join(cwd,'.gitignore') exists): compute entry as relative store path from cwd with trailing `/`; if any line trims equal to entry (or entry without trailing slash), skip; else append with leading newline when file non-empty and lacks trailing newline; never create .gitignore; second call must not duplicate
       - Return InitResult; created true when store root was newly created or config was written this call (document choice in SUMMARY)
       - Do not reimplement path confinement — use existing resolveStoreRoot / ensureStoreRoot / storeFile

    6. Create src/cli.ts (D-09 commander, D-03/D-04 K22, D-10 header):
       - Export `main(argv: string[]): number` for tests (D-11)
       - program.name('gsd-graph').showSuggestionAfterError(); program.option('--dir &lt;path&gt;', 'store directory override'); program.exitOverride()
       - Helpers: writeOk(payload) → process.stdout.write(JSON.stringify(payload)+'\n'); writeErrorJson({ok:false,reason,message}) → stderr only; never color stdout; optional picocolors on stderr only when process.stderr.isTTY
       - Register only `init` in this plan: options --ontology &lt;idOrPath&gt;; action reads global --dir via optsWithGlobals; calls init({ dir, ontology, cwd: process.cwd() }); writeOk(result)
       - try/parse/catch: GraphError → writeErrorJson + return reason===GSD_GRAPH_REASON.BUILD_LOCKED ? 3 : 2; CommanderError / other usage → writeErrorJson reason "usage" (CLI-local string, not added to GSD_GRAPH_REASON) + return 1; success return 0
       - Optional only: when require.main === module set process.exitCode = main(process.argv) so `node dist/cli.js` works for local debug — PKG-03 path is always the bin wrapper calling main (do not rely on require.main alone for the published bin)
       - Import library symbols from ./index (or relative modules) — thin adapter only (D-06)

    7. Export init, InitOptions, InitResult from src/index.ts.

    8. tests/init.test.ts (D-11 node:test): use tmpdir; require dist/cli.js main and/or dist/index.js init; assert layout + gitignore append + main exit 0 JSON stdout; assert no .gitignore creation when absent; after npm run build.
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern='init|package identity|bin'</automated>
  </verify>
  <done>gsd-graph init path works end-to-end through bin/cli/library with K22 JSON stdout and store+gitignore layout (PKG-03, CLI-02 foundation, CLI-03, D-01, D-03, D-05).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Init edge cases — custom --dir, idempotent gitignore, package bin gates</name>
  <files>src/pipeline/init.ts, tests/init.test.ts, tests/package-identity.test.ts</files>
  <behavior>
    - init with dir relative to cwd uses that path for store and gitignore entry (e.g. custom-store/) (CLI-03, D-05, D-07)
    - second init against same cwd does not duplicate gitignore line; gitignore_appended false on no-op (CLI-03)
    - package-identity asserts pkg.bin['gsd-graph'] === './bin/gsd-graph.js', files includes 'bin', bin file exists with shebang, and bin source calls main (e.g. contains main(process.argv) after require of dist/cli) (PKG-03, D-01)
    - commander remains on 14.x in package.json dependencies (D-09)
  </behavior>
  <action>
    Harden CLI-03 and PKG-03 gates (per D-01, D-05, D-07, D-09, D-11).

    1. Expand tests/init.test.ts:
       - custom --dir / InitOptions.dir relative path: store created at that path; gitignore line matches relative dir with trailing slash
       - pre-seed .gitignore containing the entry → gitignore_appended false; content unchanged (byte-stable or line-stable)
       - absolute dir option still confined by resolveStoreRoot semantics (no escape inventiveness)

    2. Extend tests/package-identity.test.ts:
       - bin field present and points at ./bin/gsd-graph.js
       - files array includes bin
       - bin/gsd-graph.js exists and first line is the node shebang
       - bin body requires dist/cli.js and invokes main (assert source includes main(process.argv) or equivalent destructure+call — not bare require alone)
       - dependencies.commander matches ^14 or 14.x (reject 15.x range)

    3. Fix any init edge bugs revealed by tests; keep algorithm in library (D-06).
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern='init|package identity|bin|gitignore'</automated>
  </verify>
  <done>Init idempotent for custom dirs; package identity enforces published bin (PKG-03, CLI-03).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: K22 exit mapper unit coverage for GraphError and usage</name>
  <files>src/cli.ts, tests/init.test.ts</files>
  <behavior>
    - main with unknown flag or missing required structure returns exit 1 and does not write success JSON on stdout (CLI-02, D-03)
    - main path that throws GraphError with reason schema_invalid (or other non-lock) returns 2 and stderr JSON has ok:false, reason, message (CLI-02, D-04)
    - main path that throws GraphError with reason build_locked returns 3 and stderr reason build_locked (CLI-02, D-03)
  </behavior>
  <action>
    Prove K22 exit mapping on the tracer CLI before remaining commands land (per D-03, D-04, D-11).

    1. In tests, invoke exported main with argv that triggers CommanderError (e.g. --not-a-real-flag on root or incomplete parse) → expect status 1.

    2. For exit 2/3 without full command surface: either (a) export a test-only hook is forbidden — prefer (b) temporarily call through init with a PATH_ESCAPE-inducing dir if feasible via library, or (c) unit-test by having main catch rethrow from a registered command. Cleanest: call init library with invalid basename patterns if PATH_ESCAPE can be forced; else add a tiny internal test helper only if unavoidable. Preferred approach matching codebase: use resolveStoreRoot-safe dirs and simulate by requiring GraphError from a deliberate bad ontology path on init if ontology load is part of init — if init does not load ontology file at write time, force exit mapping by exporting mapCliError(err): number used by main and unit-test that pure function in tests/init.test.ts (or tests/cli-exit.test.ts if cleaner). Keep mapCliError thin: GraphError build_locked→3, other GraphError→2, else→1.

    3. Ensure success path still never writes human diagnostics to stdout (JSON only).
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern='init|exit|K22|usage|build_locked'</automated>
  </verify>
  <done>Exit codes 0/1/2/3 mapping is unit-proven for the CLI adapter (CLI-02, D-03, D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| argv → CLI process | Untrusted operator/agent arguments (`--dir`, `--ontology`) |
| CLI → filesystem | Store root and `.gitignore` writes under cwd |
| npm install → node_modules | Third-party commander/picocolors |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `--dir` / init paths | high | mitigate | Only resolveStoreRoot + ensureStoreRoot + storeFile; no shell path concat (D-06) |
| T-04-02 | Information disclosure | stdout | medium | mitigate | writeOk JSON only; diagnostics/errors on stderr (D-03) |
| T-04-03 | Tampering | `.gitignore` append | low | mitigate | Append only when file exists; line membership check; never create file (D-05) |
| T-04-SC | Tampering | commander/picocolors install | high | mitigate | RESEARCH legitimacy OK; pin commander ^14.0.3 and picocolors ^1.1.1; reject commander 15 |
</threat_model>

<verification>
- `npm test` green including init + package-identity bin gates
- `node bin/gsd-graph.js init --dir /tmp/...` after build writes JSON exit 0
- package.json bin + files include bin
</verification>

<success_criteria>
- PKG-03 binary entry exists and is identity-tested
- CLI-02 exit mapper and JSON contract exist on the init path
- CLI-03 init library creates store layout and gitignore append correctly
</success_criteria>

<output>
Create `.planning/phases/04-cli-surface/04-01-SUMMARY.md` when done
</output>
