---
phase: 01-foundation-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - tsconfig.build.json
  - tsconfig.test.json
  - .gitignore
  - LICENSE
  - README.md
  - src/index.ts
  - src/errors.ts
  - src/types.ts
  - tests/package-identity.test.ts
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - PKG-01
  - PKG-02
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "npm run build emits dist/index.js and dist/index.d.ts on Node ≥22"
    - "package name is @opengsd/gsd-graph and description positions Graph Engineering toolkit"
    - "package.json has zero runtime dependency on gsd-core or @opengsd/gsd-core"
    - "require('@opengsd/gsd-graph') (or dist/index.js) exports GSD_GRAPH_REASON including PATH_ESCAPE and BUILD_LOCKED"
  artifacts:
    - path: package.json
      provides: "CJS package identity, engines, scripts, deps"
    - path: dist/index.js
      provides: "built CommonJS entry"
    - path: dist/index.d.ts
      provides: "public type declarations"
    - path: README.md
      provides: "Graph Engineering toolkit positioning"
    - path: src/errors.ts
      provides: "GSD_GRAPH_REASON + GraphError"
    - path: tests/package-identity.test.ts
      provides: "PKG-01/PKG-02 automated gates"
    - path: .github/workflows/ci.yml
      provides: "CI build+test on Node 22"
  key_links:
    - from: package.json exports["."].require
      to: dist/index.js
      via: "main/exports require condition"
    - from: tests/package-identity.test.ts
      to: package.json
      via: "reads name/description/dependencies"
    - from: src/index.ts
      to: src/errors.ts
      via: "re-exports reason codes"
---

<objective>
Bootstrap installable `@opengsd/gsd-graph` as a CJS+types TypeScript library with Graph Engineering identity, reason-code façade, copyright headers, and CI — with zero gsd-core runtime coupling.

Purpose: Later ontology and store plans need a real package build, test harness, and public error surface.
Output: package.json/tsconfig/src façade, LICENSE, README, identity tests, GitHub Actions CI.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-identity/CONTEXT.md
@.planning/phases/01-foundation-identity/RESEARCH.md
@.planning/research/STACK.md
@docs/DESIGN.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end package bootstrap — build + require reason codes</name>
  <files>package.json, package-lock.json, tsconfig.json, tsconfig.build.json, tsconfig.test.json, .gitignore, src/index.ts, src/errors.ts, src/types.ts, tests/package-identity.test.ts, LICENSE</files>
  <behavior>
    - npm run build creates dist/index.js and dist/index.d.ts
    - package.json name is @opengsd/gsd-graph; engines.node is &gt;=22.0.0
    - GSD_GRAPH_REASON exports frozen codes including ok, build_locked, schema_invalid, ontology_invalid, path_escape
    - GraphError carries a reason string from GSD_GRAPH_REASON
    - package.json dependencies and peerDependencies do not list gsd-core or @opengsd/gsd-core (D-01, PKG-02)
  </behavior>
  <action>
    Create greenfield package bootstrap per D-02/D-03/D-08/D-10 and RESEARCH Standard Stack.

    1. package.json (per D-02, D-03):
       - name `@opengsd/gsd-graph`, version `0.1.0`, license MIT
       - description must include the phrase Graph Engineering toolkit (PKG-02)
       - main `./dist/index.js`, types `./dist/index.d.ts`
       - exports["."] with types + require + default pointing at dist (CJS-only — dual ESM deferred per research discretion)
       - files: dist, schemas, ontology-packs, LICENSE, README.md
       - engines: node &gt;=22.0.0, npm &gt;=10.0.0
       - scripts: build = `tsc -p tsconfig.build.json`; test builds src then tests then `node --test` on dist-test; test:coverage wraps the test runner with c8 --check-coverage --lines 80
       - dependencies: ajv ^8.20.0, ajv-formats ^3.0.1 (install now so later plans do not fight lockfile; legitimized OK in RESEARCH)
       - devDependencies: typescript ^6.0.3, @types/node ^22.19.0, c8 ^12.0.0 (RESEARCH Package Legitimacy: SUS recency false-positives Approved — no human install checkpoint)
       - Do not add commander, zod, MCP SDK, graphology, write-file-atomic, proper-lockfile, or any gsd-core package (D-01)

    2. tsconfig.json (strict, ES2022 target, CommonJS module, Node resolution, esModuleInterop, skipLibCheck).
       tsconfig.build.json extends it: rootDir src, outDir dist, declaration true, declarationMap/sourceMap true, include src only.
       tsconfig.test.json: emit tests/**/*.ts to dist-test/, CommonJS, types node; allow resolving ../src or test against compiled dist via require of package main after build.

    3. .gitignore: node_modules, dist, dist-test, coverage, .gsd-graph, *.log, .DS_Store.

    4. LICENSE: MIT, copyright Jeremy McSpadden 2026.

    5. src/errors.ts (D-08 header `// gsd-graph — typed reason codes and GraphError`): export frozen GSD_GRAPH_REASON with at least OK, BUILD_LOCKED, BUILD_FAILED, SCHEMA_INVALID, ONTOLOGY_INVALID, EMPTY_SUBGRAPH, PROMPT_RESULT_INVALID, CORPUS_NOT_FOUND, PATH_ESCAPE, LIMIT_EXCEEDED, NO_BASELINE matching DESIGN reason-code strings (lowercase snake values). Export class GraphError extends Error with readonly reason: string and optional details.

    6. src/types.ts: placeholder shared types file with D-08 header (GraphNode/Triple interfaces may be minimal stubs filled in plan 01-02).

    7. src/index.ts: re-export GSD_GRAPH_REASON and GraphError only for this tracer; D-08 header.

    8. tests/package-identity.test.ts using node:test + node:assert/strict (D-10): assert package.json name/engines/description; assert no gsd-core in dependencies/devDependencies/peerDependencies/optionalDependencies; assert dist artifacts exist after build; assert require of dist/index.js exports GSD_GRAPH_REASON.PATH_ESCAPE and BUILD_LOCKED.

    9. Run npm install, npm run build, npm test. Confirm pins via npm ls ajv ajv-formats typescript c8 @types/node.

    Do not implement ontology loader, store IO, or CLI bin in this plan.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test -f dist/index.js &amp;&amp; test -f dist/index.d.ts &amp;&amp; npm test</automated>
  </verify>
  <done>Package builds CJS+d.ts; identity test green; reason codes requireable; no gsd-core runtime dep (PKG-01, PKG-02, D-01..D-03, D-08, D-10).</done>
</task>

<task type="auto">
  <name>Task 2: README Graph Engineering identity (PKG-02)</name>
  <files>README.md</files>
  <action>
    Write README.md that positions the package as a standalone Graph Engineering toolkit per D-01/D-02 and PKG-02.

    Required content:
    - Title and npm name `@opengsd/gsd-graph`, CLI name `gsd-graph` (CLI lands Phase 4 — mention as planned binary name only)
    - One-paragraph product pitch: extract → normalize → store → query → ground → maintain over file-first graph.v1.json under `.gsd-graph/`
    - Explicit statement that OpenGSD is publisher namespace only and this package has no runtime dependency on gsd-core, GSD workflows, or host `.planning/` integration
    - Requirements: Node ≥22, `npm install @opengsd/gsd-graph`, `npm run build` / `npm test` for contributors
    - Store defaults: `.gsd-graph/`, SoT `graph.v1.json`, optional disposable `graph.json` projection (D-04) — note projection never read as SoT
    - Link to docs/DESIGN.md for full design
    - Do not describe the package as a gsd-core plugin, capability, or graphify migration tool

    Extend tests/package-identity.test.ts to assert README.md contains "Graph Engineering" and does not claim gsd-core runtime integration (read file text; ban phrases that assert runtime coupling to gsd-core workflows).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>README presents Graph Engineering toolkit identity with zero gsd-core runtime coupling (PKG-02, D-01, D-02).</done>
</task>

<task type="auto">
  <name>Task 3: GitHub Actions CI on Node 22</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Add `.github/workflows/ci.yml` (research discretion: GitHub Actions):
    - triggers: push and pull_request
    - job on ubuntu-latest with Node 22 (matrix may include 22 and 24 optional; Node 22 required)
    - steps: checkout, setup-node with npm cache, npm ci, npm run build, npm test
    - Do not require coverage gate in CI yet if it slows greenfield; optional npm run test:coverage once tests exist

    Confirm every file under src/ starts with the two-line D-08 copyright header block (gsd-graph purpose line + Copyright (c) 2026 Jeremy McSpadden &lt;jeremy@fluxlabs.net&gt;).
  </action>
  <verify>
    <automated>npm test &amp;&amp; test -f .github/workflows/ci.yml &amp;&amp; grep -q "node-version" .github/workflows/ci.yml &amp;&amp; grep -q "npm test" .github/workflows/ci.yml</automated>
  </verify>
  <done>CI workflow runs build+test on Node ≥22; copyright headers present on source files (D-03, D-08, D-10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → install | Third-party packages enter the dependency graph at install |
| package metadata → consumers | name/description/exports define what hosts trust about identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | npm installs (ajv, typescript, c8, …) | high | mitigate | Only RESEARCH-audited packages; pins in package.json; no postinstall scripts; legitimacy OK/Approved |
| T-01-02 | Spoofing | package name / README | medium | mitigate | Exact `@opengsd/gsd-graph` name; README denies gsd-core runtime coupling (PKG-02) |
| T-01-03 | Information disclosure | published files | low | mitigate | package.json files whitelist (dist, schemas, ontology-packs, LICENSE, README) — no .planning |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | Package Legitimacy Audit already run; SUS recency false-positives Approved in RESEARCH — no [SLOP] |
</threat_model>

<verification>
- `npm run build` produces dist/index.js + dist/index.d.ts
- `npm test` runs package-identity tests green
- package.json description includes Graph Engineering toolkit
- No gsd-core package in any dependency class
</verification>

<success_criteria>
PKG-01 and PKG-02 satisfied: installable CJS+types package with GE toolkit docs and standalone identity.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-identity/01-01-SUMMARY.md` when done
</output>
