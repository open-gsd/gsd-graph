---
phase: 01-foundation-identity
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/io/paths.ts
  - src/io/lock.ts
  - src/io/atomic-publish.ts
  - src/io/safe-json.ts
  - src/io/load-graph.ts
  - src/index.ts
  - tests/paths-confine.test.ts
  - tests/lock.test.ts
  - tests/publish-dual-write.test.ts
autonomous: true
requirements:
  - STORE-01
  - STORE-02
  - STORE-03
  - STORE-04
  - STORE-05
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "resolveStoreRoot defaults to .gsd-graph under cwd and honors dir override and GSD_GRAPH_DIR"
    - "confineUnderRoot rejects .. and symlink escape with PATH_ESCAPE"
    - "acquireBuildLock exclusive wx; second holder gets BUILD_LOCKED; stale/dead PID steal works"
    - "publishGraphFiles renames graph.v1.json before optional graph.json; loadGraphV1 never treats projection as SoT"
    - "store.write_projection product default is false (research discretion); projection still supported when requested"
  artifacts:
    - path: src/io/paths.ts
      provides: "resolveStoreRoot + realpath confinement"
    - path: src/io/lock.ts
      provides: ".build.lock acquire/release/steal"
    - path: src/io/atomic-publish.ts
      provides: "dual-write ordered publish"
    - path: src/io/load-graph.ts
      provides: "v1-only load API"
    - path: tests/paths-confine.test.ts
      provides: "STORE-01/05"
    - path: tests/lock.test.ts
      provides: "STORE-04"
    - path: tests/publish-dual-write.test.ts
      provides: "STORE-02/03"
  key_links:
    - from: publishGraphFiles
      to: graph.v1.json
      via: "tmp fsync rename v1 first (D-06)"
    - from: loadGraphV1
      to: graph.v1.json only
      via: "refuses graph.json as SoT (D-04)"
    - from: confineUnderRoot
      to: store realpath root
      via: "prefix check → PATH_ESCAPE (D-07)"
    - from: acquireBuildLock
      to: .build.lock
      via: "open wx exclusive (D-06, STORE-04)"
---

<objective>
Implement realpath-confined store paths, exclusive `.build.lock`, dual-write publish (v1-first rename), and a load API that never treats `graph.json` as source of truth.

Purpose: Crash-safe multi-file store primitives unblock the Phase 2 build pipeline.
Output: src/io/* modules, path/lock/publish tests, public exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-identity/CONTEXT.md
@.planning/phases/01-foundation-identity/RESEARCH.md
@.planning/phases/01-foundation-identity/01-01-SUMMARY.md
@.planning/phases/01-foundation-identity/01-02-SUMMARY.md
@docs/DESIGN.md
@src/errors.ts
@src/schema/validators.ts
@src/index.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end store path resolve + publish one graph.v1</name>
  <files>src/io/paths.ts, src/io/atomic-publish.ts, src/io/safe-json.ts, src/io/load-graph.ts, src/index.ts, tests/paths-confine.test.ts, tests/publish-dual-write.test.ts</files>
  <behavior>
    - resolveStoreRoot() without opts ends with path segment .gsd-graph (STORE-01, D-02)
    - resolveStoreRoot({ dir: custom }) and GSD_GRAPH_DIR override default
    - After ensure store dir exists, publishGraphFiles with writeProjection false writes graph.v1.json only (no graph.json)
    - loadGraphV1 reads that file and returns validated document
    - If only graph.json exists and graph.v1.json is missing, loadGraphV1 fails SCHEMA_INVALID (or dedicated missing-SoT mapping to schema_invalid) — never invents graph from projection (D-04, STORE-02)
    - confineUnderRoot rejects candidate that realpath-escapes root with PATH_ESCAPE (STORE-05, D-07)
  </behavior>
  <action>
    Implement STORE-01/02/03/05 path + dual-write tracer per D-04, D-06, D-07. Hand-roll only — do not add write-file-atomic (RESEARCH Don't Hand-Roll dual-write exception table says hand-roll).

    1. src/io/paths.ts (D-08 header):
       - DEFAULT_STORE_DIR = '.gsd-graph'
       - resolveStoreRoot({ dir?, env?, cwd? }): raw = dir ?? env.GSD_GRAPH_DIR ?? DEFAULT_STORE_DIR; path.resolve(cwd, raw); if path does not exist yet, return resolved absolute path without requiring realpath of missing dir OR mkdir in ensure helper — document choice. When path exists, return fs.realpathSync.native(abs).
       - ensureStoreRoot(storeRoot): mkdir recursive for store root
       - confineUnderRoot(rootReal, candidate): resolve + realpath when exists; require resolved === root or startsWith root+sep; else throw GraphError(PATH_ESCAPE). For not-yet-created children, resolve path.normalize and still reject .. escape via relative/prefix checks before create.
       - storeFile(storeRoot, name): confineUnderRoot for known basenames (graph.v1.json, graph.json, .build.lock, .last-build-status.json)

    2. src/io/safe-json.ts: readJsonFile / writeJsonAtomicTemp helpers used by publish (write UTF-8 JSON with trailing newline optional; parse with JSON.parse).

    3. src/io/atomic-publish.ts — publishGraphFiles(plan):
       PublishPlan: { storeRoot, graphV1: object, projection?: object | null, sidecars?: Record&lt;string, object&gt;, writeProjection: boolean }
       Protocol (DESIGN dual-write, D-06) assuming caller holds lock when used in full pipeline; Phase 1 may acquire lock inside or require held lock — pick require-caller-holds-lock and document; tests will acquire lock around publish once lock module lands in Task 2, or Task 1 temporarily publishes without lock and Task 2 wires lock. Prefer: publishGraphFiles does not acquire lock itself (single responsibility); tests call publish under lock after Task 2. For Task 1 tracer tests, call publishGraphFiles alone for rename-order unit tests.
       Steps:
       a. Optionally validate graphV1 with validateGraphV1; on fail throw SCHEMA_INVALID
       b. Write graph.v1.json via tmp name `graph.v1.json.tmp-${pid}-${counter}` under storeRoot (confined), fsync file, do not rename yet
       c. If writeProjection true and projection provided, write graph.json.tmp-* similarly
       d. Write sidecar temps if any
       e. renameSync tmp v1 → graph.v1.json FIRST
       f. then projection if any, then sidecars
       g. write .last-build-status.json { status: 'ok', reason: 'ok', finished_at: ISO }
       h. On failure: best-effort unlink temps; write status failed when possible; rethrow
       Product default: callers should pass writeProjection false unless config enables it (research discretion default false — D-04 projection disposable). Export constant DEFAULT_WRITE_PROJECTION = false.

    4. src/io/load-graph.ts — loadGraphV1(storeRoot):
       - Resolve confined path to graph.v1.json only
       - If missing: throw GraphError(SCHEMA_INVALID) — do not fall back to graph.json (D-04, STORE-02, STORE-03)
       - Read JSON, validateGraphV1, return document

    5. Export resolveStoreRoot, confineUnderRoot, DEFAULT_STORE_DIR, publishGraphFiles, loadGraphV1, DEFAULT_WRITE_PROJECTION from src/index.ts.

    6. tests/paths-confine.test.ts: default dir, override dir, env override, PATH_ESCAPE for `..` and symlink escape fixture (skip symlink case if OS lacks permission, but implement and run on macOS/Linux CI).

    7. tests/publish-dual-write.test.ts: temp directory store; publish minimal valid graph.v1 (use validateGraphV1-accepted fixture from DESIGN canonical fields + empty or one node/triple as schema allows); assert graph.v1.json exists; with writeProjection false assert graph.json absent; with writeProjection true assert both exist and v1 content authoritative; assert loadGraphV1 works; assert load fails when only projection file present (write projection file manually, delete v1).

    Optional rename-order test: spy/wrapper or instrument by writing large files is unnecessary if step order is unit-tested via intermediate observation — at minimum assert final state and missing-v1-only-projection failure.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Store default .gsd-graph, realpath confinement, v1-first publish, load refuses projection as SoT (STORE-01/02/03/05, D-02/D-04/D-06/D-07).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exclusive .build.lock with stale steal (STORE-04)</name>
  <files>src/io/lock.ts, src/index.ts, tests/lock.test.ts</files>
  <behavior>
    - acquireBuildLock succeeds and creates .build.lock JSON with pid, started_at, owner, cwd
    - second acquireBuildLock without release throws GraphError BUILD_LOCKED
    - release() unlinks lock and allows re-acquire
    - stale lock: started_at older than 15 minutes OR dead pid → steal succeeds
    - live non-stale foreign pid → BUILD_LOCKED (fail-fast default)
  </behavior>
  <action>
    Implement hand-rolled lock per D-06, STORE-04, DESIGN build locking table. Do not use proper-lockfile.

    src/io/lock.ts:
    - BuildLockPayload { pid, started_at ISO, owner: 'cli'|'lib'|'mcp'|'test', cwd }
    - LockHandle { release(): void }
    - STALE_MS = 15 * 60 * 1000
    - acquireBuildLock(storeRoot, owner, opts?: { waitMs?: number }):
      1. ensure store root exists; lock path = confine join `.build.lock`
      2. try fs.openSync(lockPath, 'wx') — exclusive create
      3. on success write JSON payload, close, return handle whose release unlinks lock (idempotent)
      4. on EEXIST: read payload; if stale by age OR process.kill(pid, 0) throws (dead PID), unlink and retry once (steal); else throw GraphError(BUILD_LOCKED)
      5. waitMs optional: simple sleep/retry loop until waitMs elapses then BUILD_LOCKED — keep small and tested; default waitMs 0 fail-fast
    - Use try/finally patterns so crashed tests can still clean locks in after hooks

    Export acquireBuildLock from src/index.ts.

    tests/lock.test.ts: temp store; acquire/release; contention; stale steal by writing old started_at with non-existent pid; do not kill live processes in tests.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Exclusive .build.lock with fail-fast contention and stale/dead-PID steal (STORE-04, D-06).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire lock + publish contract tests (dual-write under lock)</name>
  <files>src/io/atomic-publish.ts, src/index.ts, tests/publish-dual-write.test.ts, tests/lock.test.ts</files>
  <behavior>
    - Documented usage: acquireBuildLock → publishGraphFiles → release
    - publish with writeProjection true leaves readable graph.v1.json even if test deletes graph.json afterward — loadGraphV1 still works (projection disposable)
    - concurrent second lock during held lock fails BUILD_LOCKED
    - DEFAULT_WRITE_PROJECTION is false
  </behavior>
  <action>
    Close STORE-02/03 integration gaps and export surface:

    1. Add test: withBuildLock helper pattern in test file — acquire, publish minimal graph with writeProjection true, release; assert loadGraphV1; delete graph.json; loadGraphV1 still succeeds (D-04).

    2. Add test: hold lock in outer scope; nested acquireBuildLock throws BUILD_LOCKED while held (STORE-04 × publish path).

    3. Ensure src/index.ts public façade exports: resolveStoreRoot, confineUnderRoot, DEFAULT_STORE_DIR, DEFAULT_WRITE_PROJECTION, acquireBuildLock, publishGraphFiles, loadGraphV1, plus prior ontology/schema/error exports from plans 01-01/01-02.

    4. README short "Library foundation" note optional one-liner that store helpers exist — only if already editing README; not required if package-identity tests would churn.

    5. Run full npm test and npm run test:coverage; if coverage gate fails only on unreachable Windows branches, adjust c8 exclude or add branch tests — keep lines ≥80 on Phase 1 modules where practical.

    Do not implement extract/normalize/CLI (deferred).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Lock + dual-write integration green; public IO API complete; Phase 1 store success criteria 3–4 met (STORE-01..05, D-04, D-06, D-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller paths → store root | Untrusted dir/env/symlink inputs cross into filesystem IO |
| multi-writer processes | Concurrent Node processes may race on the same store |
| publish temps → final names | Crash mid-rename must not promote projection over v1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Elevation of privilege | src/io/paths.ts | high | mitigate | realpath + prefix; GraphError PATH_ESCAPE (D-07, STORE-05) |
| T-01-09 | Tampering | dual-write publish | high | mitigate | v1 rename first; loadGraphV1 ignores projection (D-04, D-06, STORE-02/03) |
| T-01-10 | Tampering | concurrent writers | high | mitigate | .build.lock wx exclusive + stale steal (STORE-04) |
| T-01-11 | Denial of service | lock held forever | medium | mitigate | 15m stale or dead-PID steal |
| T-01-12 | Information disclosure | lock cwd field | low | accept | local single-user store; cwd is operational metadata |
| T-01-SC | Tampering | npm installs | high | mitigate | No new runtime deps; hand-rolled lock/publish only |
</threat_model>

<verification>
- npm test: paths-confine, lock, publish-dual-write all green
- Default store `.gsd-graph`; PATH_ESCAPE on escape fixtures
- Dual-write v1-first; load never uses projection as SoT
- Lock contention and stale steal covered
</verification>

<success_criteria>
STORE-01..05 satisfied; ROADMAP Phase 1 success criteria 3 and 4 true alongside prior plans' criteria 1–2.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-identity/01-03-SUMMARY.md` when done
</output>
