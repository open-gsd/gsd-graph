---
phase: 06-optional-agents
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - ontology-packs/research/ontology.json
  - ontology-packs/research/README.md
  - ontology-packs/engineering/ontology.json
  - ontology-packs/engineering/README.md
  - tests/ontology-examples.test.ts
autonomous: true
requirements:
  - ONT-04
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "loadOntologyPack({ packIdOrPath: 'research' }) and 'engineering' succeed replace-only with closed type/predicate allowlists (D-09, ONT-04)"
    - "research pack includes DESIGN domain types/predicates (Paper, Author, Method, Dataset; cites, evaluates, uses_method) (D-09)"
    - "engineering pack includes DESIGN domain types/predicates (Service, Incident, Decision, Change, API; depends_on, owns, mitigates, deploys) (D-09)"
    - "Neither pack includes extends; loader still rejects extends (ONT-03, D-09)"
    - "Each pack ships README documenting domain use and replace-only copy workflow like general (D-09)"
  artifacts:
    - path: ontology-packs/research/ontology.json
      provides: "research closed allowlist pack"
    - path: ontology-packs/engineering/ontology.json
      provides: "engineering closed allowlist pack"
    - path: ontology-packs/research/README.md
      provides: "research domain docs"
    - path: ontology-packs/engineering/README.md
      provides: "engineering domain docs"
    - path: tests/ontology-examples.test.ts
      provides: "load + shape gates for both packs"
  key_links:
    - from: loadOntologyPack('research')
      to: ontology-packs/research/ontology.json
      via: "package-shipped packId path (Phase 1 loader)"
    - from: loadOntologyPack('engineering')
      to: ontology-packs/engineering/ontology.json
      via: "package-shipped packId path"
---

<objective>
Ship example replace-only ontology packs `research` and `engineering` with DESIGN type/predicate tables, READMEs, and load tests (ONT-04, D-09).

Purpose: Domain starters for agent hosts without pack composition (`extends` remains unsupported).
Output: `ontology-packs/research/*`, `ontology-packs/engineering/*`, `tests/ontology-examples.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-optional-agents/CONTEXT.md
@.planning/phases/06-optional-agents/RESEARCH.md
@docs/DESIGN.md
@ontology-packs/general/ontology.json
@ontology-packs/general/README.md
@src/ontology/load-pack.ts
@tests/ontology-load.test.ts
@schemas/ontology-pack.schema.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end load research pack via pack id</name>
  <files>ontology-packs/research/ontology.json, ontology-packs/research/README.md, tests/ontology-examples.test.ts</files>
  <behavior>
    - loadOntologyPack({ packIdOrPath: 'research' }) returns LoadedOntology with id research
    - node_types include Paper, Author, Method, Dataset (and practical baseline types per RESEARCH suggested pack)
    - predicates include cites, evaluates, uses_method (plus related_to/same_as baseline)
    - strict true; unknown_*_policy review; no extends property
    - validateOntologyPack / loader Ajv accept the file
  </behavior>
  <action>
    Create `ontology-packs/research/ontology.json` as full replace-only pack matching RESEARCH suggested research pack (D-09). Do not add `extends`. Shape mirrors general: id, version, title, node_types, predicates with domain/range, strict, unknown_predicate_policy, unknown_type_policy.

    Create `ontology-packs/research/README.md` mirroring general README: domain purpose, replace-only copy workflow, explicit note that extends is unsupported (D-09).

    Tests in `tests/ontology-examples.test.ts`: load by id research; assert DESIGN types/predicates present; assert no extends key in raw JSON; assert packHash defined if loader provides it.
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test dist-test/ontology-examples.test.js</automated>
  </verify>
  <done>research pack loads replace-only with DESIGN research types/predicates and README (D-09, ONT-04 partial).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Engineering pack + dual-pack suite gates</name>
  <files>ontology-packs/engineering/ontology.json, ontology-packs/engineering/README.md, tests/ontology-examples.test.ts</files>
  <behavior>
    - loadOntologyPack({ packIdOrPath: 'engineering' }) succeeds
    - types include Service, Incident, Decision, Change, API
    - predicates include depends_on, owns, mitigates, deploys
    - both packs load in one test file; extends still rejected if injected (existing loader behavior)
  </behavior>
  <action>
    Create `ontology-packs/engineering/ontology.json` per RESEARCH suggested engineering pack (D-09) — full replace-only, no extends, strict + review policies like general.

    Create engineering README same structure as research/general.

    Expand tests for engineering DESIGN coverage and that both packs load without network. Confirm package already ships `ontology-packs` in files (no package.json change required unless missing).

    Do not implement communities or pack extends (deferred).
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test dist-test/ontology-examples.test.js dist-test/ontology-load.test.js &amp;&amp; npm test</automated>
  </verify>
  <done>ONT-04 complete: research + engineering packs load with docs; suite green (D-09, D-10, D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shipped pack JSON → loader | Trusted package assets validated by Ajv + replace-only rules |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-14 | Tampering | ontology pack content | low | mitigate | Ajv ontology-pack schema + loader extends rejection |
| T-06-15 | Elevation of privilege | accidental ambient ontology expand | medium | mitigate | strict true + unknown policy review; replace-only only |
| T-06-SC | Tampering | npm installs | low | accept | No new packages |
</threat_model>

<verification>
- ontology-examples.test.js loads research + engineering
- ontology-load.test.js still green
- npm test green
</verification>

<success_criteria>
- ONT-04: example research and engineering packs with READMEs load like general
- D-09 honored; no extends; no communities
</success_criteria>

<output>
Create `.planning/phases/06-optional-agents/06-04-SUMMARY.md` when done
</output>
