---
phase: 06-optional-agents
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - package.json
  - package-lock.json
  - bin/gsd-graph-mcp.js
  - src/mcp/server.ts
  - src/mcp/tools.ts
  - tests/mcp-tools.test.ts
  - tests/package-identity.test.ts
autonomous: false
requirements:
  - MCP-01
estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Default MCP tool list includes graph_status, graph_query, graph_pack, graph_answer, graph_review_list and excludes graph_build and graph_review_resolve (D-06, MCP-01)"
    - "When allow_build / allow_review_write flags or config true, write tools register and call public build / reviewResolve (D-06)"
    - "MCP bin gsd-graph-mcp is published and uses @modelcontextprotocol/sdk 1.x + zod for tool input schemas (D-07)"
    - "Tool handlers call public library APIs only; loadGraphV1 / status / query / pack / answer — never projection as SoT (D-10)"
    - "MCP process does not require live network for tool unit tests (D-12)"
  artifacts:
    - path: src/mcp/server.ts
      provides: "createMcpServer / start with default-off write gates"
    - path: src/mcp/tools.ts
      provides: "tool handlers mapping to library"
    - path: bin/gsd-graph-mcp.js
      provides: "stdio entry shebang"
    - path: tests/mcp-tools.test.ts
      provides: "registration matrix + default-off writes"
  key_links:
    - from: createMcpServer
      to: server.tool graph_*
      via: "write tools only if allowBuild / allowReviewWrite (D-06)"
    - from: graph_pack / graph_answer
      to: packSubgraph / answer
      via: "public index exports"
    - from: bin/gsd-graph-mcp.js
      to: dist mcp server start
      via: "dynamic import SDK subpaths for CJS (RESEARCH Pattern 4)"
---

<objective>
Ship optional MCP stdio server (`bin/gsd-graph-mcp.js`) with read tools on by default and build/review-write off by default, using `@modelcontextprotocol/sdk` ^1.30.0 and `zod` ^4 as normal dependencies (MCP-01, D-06, D-07, D-12).

Purpose: Agent hosts (Claude/Cursor) can query/pack/answer without accidental store mutation.
Output: MCP server modules, bin entry, package.json bin+deps, registration tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-optional-agents/CONTEXT.md
@.planning/phases/06-optional-agents/RESEARCH.md
@docs/DESIGN.md
@src/index.ts
@src/pipeline/status.ts
@src/pipeline/query.ts
@src/pipeline/pack.ts
@src/pipeline/answer.ts
@src/pipeline/review.ts
@src/pipeline/build.ts
@bin/gsd-graph.js
@package.json
@tests/package-identity.test.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm @modelcontextprotocol/sdk 1.x package legitimacy</name>
  <what-built>
    RESEARCH Package Legitimacy Audit marked `@modelcontextprotocol/sdk@1.30.0` as [SUS] solely for recent publish date, with disposition **Approved for use** (official org, ~53M/wk, open-gsd already on ^1.27.1). `zod@^4` is [OK]. No install has run yet in this plan.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@modelcontextprotocol/sdk and confirm latest 1.x is the official Model Context Protocol TypeScript SDK (not a typosquat).
    2. Confirm you want pin range `^1.30.0` (1.x line) — not `@modelcontextprotocol/server` 2.x.
    3. Confirm `zod@^4.0.0` as peer for tool inputSchema.
    Reply **approved** to proceed with `npm install`, or describe issues.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: End-to-end MCP graph_status tool + bin scaffold</name>
  <files>package.json, package-lock.json, src/mcp/server.ts, src/mcp/tools.ts, bin/gsd-graph-mcp.js, tests/mcp-tools.test.ts, tests/package-identity.test.ts</files>
  <behavior>
    - After install, createMcpServer (or listRegisteredTools helper) default tool names include graph_status
    - graph_status handler returns JSON from public status() for a temp store dir
    - bin field gsd-graph-mcp → ./bin/gsd-graph-mcp.js; shebang #!/usr/bin/env node
    - No graph_build in default registration (D-06)
  </behavior>
  <action>
    After approval: `npm install @modelcontextprotocol/sdk@^1.30.0 zod@^4.0.0` as **dependencies** (not optionalDependencies) per D-07 and RESEARCH discretion. Keep engines node &gt;=22.

    Implement `src/mcp/server.ts` + `src/mcp/tools.ts`: create server named `gsd-graph` version from package; use dynamic import of `@modelcontextprotocol/sdk/server/mcp.js` and `server/stdio.js` for CJS emit compatibility (RESEARCH Pattern 4 / open-gsd pattern). Register at least `graph_status` with zod optional `dir` input calling public `status()` (D-06, D-11).

    Export a testable `listToolNames(opts)` or `createGsdGraphMcpServer(opts)` that returns registered tool names without connecting stdio when possible so unit tests avoid hanging on stdin.

    Add `bin/gsd-graph-mcp.js` shebang + require/start dist MCP entry (mirror gsd-graph.js style). Add package.json `bin.gsd-graph-mcp`.

    Extend package-identity tests: bin key present; shebang; dependencies include `@modelcontextprotocol/sdk` matching 1.x (not 2.x package names).

    Tests: `tests/mcp-tools.test.ts` — default names include graph_status and exclude graph_build (D-06, D-12).
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test dist-test/mcp-tools.test.js dist-test/package-identity.test.js</automated>
  </verify>
  <done>MCP server registers graph_status via library status(); bin published; default excludes build (D-06, D-07).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Full read tool matrix + default-off write tools</name>
  <files>src/mcp/server.ts, src/mcp/tools.ts, tests/mcp-tools.test.ts</files>
  <behavior>
    - Default tools: graph_status, graph_query, graph_pack, graph_answer, graph_review_list (D-06)
    - graph_build only when allowBuild true (flag --allow-build or mcp.allow_build)
    - graph_review_resolve only when allowReviewWrite true
    - Handlers use public query / packSubgraph / answer / loadReviewQueue|list pending / build / reviewResolve — never graph.json projection as SoT
    - Tool handlers must not write diagnostics to stdout (stdio JSON-RPC integrity)
  </behavior>
  <action>
    Register remaining read tools with zod inputSchema shapes covering dir, question, term/path/neighborhood fields as needed for thin wrappers (D-06, MCP-01). Use DESIGN `graph_*` names (discretion resolved).

    Gate `graph_build` and `graph_review_resolve` behind `allowBuild` / `allowReviewWrite` options from CLI argv and/or config; **do not register** when false (D-06). Document privileged nature in tool descriptions.

    graph_review_list: pending items from loadReviewQueue (or existing review list helper) — read only.

    Bin/CLI flags: parse `--allow-build`, `--allow-review-write`, `--dir` on MCP entry without polluting stdout.

    Expand tests: default list exact set; with allow flags includes write tools; one integration-style call that graph_pack/graph_answer delegate returns pack/answer-shaped JSON on fixture graph (D-12 offline).
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test dist-test/mcp-tools.test.js &amp;&amp; npm test</automated>
  </verify>
  <done>MCP-01 complete: read tools default on; build and review-write off unless explicitly enabled (D-06); suite green offline (D-10, D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP host agent → stdio tools | Host may call any registered tool; write tools are privileged |
| Tool args → library | Untrusted strings (dir, corpus, question) cross into FS/API |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Elevation of privilege | graph_build / graph_review_resolve | high | mitigate | Do not register unless allow flags/config true (D-06) |
| T-06-08 | Tampering | tool dir / corpus paths | high | mitigate | Reuse library resolveStoreRoot / build confinement |
| T-06-09 | Information disclosure | read tools over local store | medium | accept | Local stdio trust model; documented in DESIGN |
| T-06-10 | Denial of service | stdio stdout pollution | medium | mitigate | No console.log on stdout from handlers; stderr only |
| T-06-SC | Tampering | @modelcontextprotocol/sdk install | high | mitigate | blocking-human legitimacy checkpoint + pin official 1.x package name |
</threat_model>

<verification>
- Default tool list excludes write tools
- Allow flags enable write tools
- package-identity bin + dep pins
- Full `npm test` offline
</verification>

<success_criteria>
- MCP-01: status/query/pack/answer (+ review list) available; build/review-write off by default
- D-06, D-07, D-11, D-12 satisfied
</success_criteria>

<output>
Create `.planning/phases/06-optional-agents/06-02-SUMMARY.md` when done
</output>
