---
phase: 06-optional-agents
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/pipeline/report.ts
  - src/cli.ts
  - src/index.ts
  - src/pipeline/build.ts
  - tests/report.test.ts
  - tests/cli-commands.test.ts
autonomous: true
requirements:
  - RPT-01
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "writeGraphReport loads published graph.v1 only and writes GRAPH_REPORT.md with node/triple counts and top predicates (D-08, RPT-01)"
    - "Report markdown states it is non-authoritative; query/pack/answer still use graph.v1 as SoT (D-08, D-10)"
    - "CLI gsd-graph report invokes writeGraphReport and emits K22 JSON with path and counts"
    - "report.write_on_build defaults false; optional true writes after successful publish without failing build on report error (discretion)"
  artifacts:
    - path: src/pipeline/report.ts
      provides: "writeGraphReport from loadGraphV1"
    - path: tests/report.test.ts
      provides: "counts + top predicates + v1-only load"
  key_links:
    - from: writeGraphReport
      to: loadGraphV1
      via: "resolveStoreRoot then loadGraphV1 — never projection (D-08, D-10)"
    - from: writeGraphReport
      to: storeFile(root, 'GRAPH_REPORT.md')
      via: "confined store basename write"
    - from: CLI report
      to: writeGraphReport
      via: "K22 writeOk JSON"
---

<objective>
Ship minimal `GRAPH_REPORT.md` writer from published `graph.v1.json` (counts + top predicates), library export, explicit CLI `report`, and optional non-default write-on-build (RPT-01, D-08, D-10).

Purpose: Humans and agents get a disposable summary without treating report as SoT.
Output: `src/pipeline/report.ts`, CLI verb, tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-optional-agents/CONTEXT.md
@.planning/phases/06-optional-agents/RESEARCH.md
@docs/DESIGN.md
@src/io/load-graph.ts
@src/io/paths.ts
@src/pipeline/status.ts
@src/pipeline/build.ts
@src/cli.ts
@src/index.ts
@tests/status.test.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end writeGraphReport from fixture v1</name>
  <files>src/pipeline/report.ts, src/index.ts, tests/report.test.ts</files>
  <behavior>
    - Given a temp store with valid graph.v1.json, writeGraphReport writes GRAPH_REPORT.md under store
    - File includes non-authoritative notice, engine/ontology/built_at when present, node_count, triple_count, top N predicates by frequency (stable tie-break by predicate id asc)
    - Returns { path, node_count, triple_count }
    - Uses loadGraphV1 only (D-08, D-10)
  </behavior>
  <action>
    Implement `writeGraphReport(opts?: { dir?: string; topN?: number })` in `src/pipeline/report.ts` per RESEARCH Pattern 7 and D-08. Default topN=10. Sort predicates by count desc then id asc. Header must state non-authoritative / SoT is graph.v1.json.

    Optional: include pending review count from loadReviewQueue if queue file exists — do not fail if missing.

    Export from `src/index.ts`. Copyright D-11.

    Tests write a minimal GraphV1Document via existing publish/load helpers or direct v1 write matching schema, then assert markdown contents and return shape. Assert load path is v1 (no dependency on graph.json content for counts).
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test dist-test/report.test.js</automated>
  </verify>
  <done>GRAPH_REPORT.md written from v1 with counts + top predicates (RPT-01, D-08).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI report + optional write_on_build hook</name>
  <files>src/cli.ts, src/pipeline/build.ts, tests/report.test.ts, tests/cli-commands.test.ts</files>
  <behavior>
    - `gsd-graph report` (main argv) exit 0 JSON with path and counts when v1 exists
    - Missing v1 maps through mapCliError (schema/no graph) non-zero appropriately
    - build does not write report when report.write_on_build is false/default
    - when config or option enables write_on_build, report written after successful publish; report failure does not fail build (discretion resolved)
  </behavior>
  <action>
    Register CLI `report` command with global `--dir`, call writeGraphReport, writeOk K22 JSON (same patterns as status).

    Wire optional build hook: read config `report.write_on_build` default **false** (D-08 / RESEARCH discretion). If true after successful publishGraphFiles while lock held or immediately after, call writeGraphReport; catch errors and record diagnostic without throwing build failure.

    Extend tests for CLI report via main(argv) capture and write_on_build default false (build test or unit on build opts).
  </action>
  <verify>
    <automated>npm run build:test &amp;&amp; node --test dist-test/report.test.js dist-test/cli-commands.test.js &amp;&amp; npm test</automated>
  </verify>
  <done>CLI report works; write_on_build default false; full suite green; report never SoT (D-08, D-10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Report file → human/agent readers | Disposable markdown; must not be treated as graph SoT |
| build → report writer | Optional side effect after publish |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-11 | Tampering | GRAPH_REPORT.md mistaken for SoT | medium | mitigate | Non-authoritative header; loadGraphV1 for data; query paths unchanged (D-08) |
| T-06-12 | Tampering | report path escape | high | mitigate | storeFile basename GRAPH_REPORT.md under store root |
| T-06-13 | Denial of service | write_on_build report failure | low | mitigate | Report errors do not fail build when optional |
| T-06-SC | Tampering | npm installs | low | accept | No new packages in this plan |
</threat_model>

<verification>
- report.test.js asserts counts + top predicates from v1
- CLI report K22 exit 0
- npm test green; goldens unaffected
</verification>

<success_criteria>
- RPT-01 complete: minimal GRAPH_REPORT from published v1
- D-08, D-10, D-11, D-12 honored
</success_criteria>

<output>
Create `.planning/phases/06-optional-agents/06-03-SUMMARY.md` when done
</output>
