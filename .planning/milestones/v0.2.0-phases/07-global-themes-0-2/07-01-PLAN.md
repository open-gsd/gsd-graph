---
phase: 07-global-themes-0-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/pipeline/communities.ts
  - src/index.ts
  - tests/communities.test.ts
autonomous: true
requirements:
  - COM-01
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "detectCommunities on a synthetic two-clique GraphV1Document returns exactly two communities of size ≥3 with correct partition (COM-01, D-01, D-02, D-10)"
    - "Only EXTRACTED and INFERRED triples form undirected community edges; AMBIGUOUS bridges never merge cliques (D-03)"
    - "Label propagation is pure TypeScript, max 20 iterations default, min community size 3 default, deterministic across two runs (D-01, D-02, D-05, D-10)"
    - "Public façade exports detectCommunities, constants, and Community types with copyright headers on new sources (D-09)"
  artifacts:
    - path: src/pipeline/communities.ts
      provides: "projectCommunityEdges, labelPropagation, finalizeCommunities, detectCommunities (write optional later)"
    - path: src/types.ts
      provides: "Community, DetectCommunitiesOptions, DetectCommunitiesResult"
    - path: tests/communities.test.ts
      provides: "two-clique, AMBIGUOUS bridge, min-size, max-iter, determinism gates"
    - path: src/index.ts
      provides: "public re-exports for communities API"
  key_links:
    - from: detectCommunities({ graph })
      to: projectCommunityEdges → labelPropagation → finalizeCommunities
      via: "pure pipeline; no FS when graph injected and write false (D-01, D-10)"
    - from: projectCommunityEdges
      to: confidenceRank(t.confidence) >= confidenceRank('INFERRED')
      via: "ids.ts shared tier table (D-03)"
    - from: finalizeCommunities
      to: "c_NNNN ids + stable_key sha256"
      via: "size desc then min(memberId) asc sort (research Pattern 1)"
---

<objective>
Implement pure-TypeScript label-propagation community detection as a library pipeline over an injected or loaded GraphV1Document, with offline synthetic tests proving two-clique structure, confidence filtering, min-size, max iterations, and determinism (COM-01 core algorithm; D-01, D-02, D-03, D-05, D-09, D-10).

Purpose: Global theme discovery is the 0.2.0 differentiator; algorithm must be offline, dep-free, and bit-stable before artifact/CLI layers land.
Output: `src/pipeline/communities.ts`, Community types, public exports, `tests/communities.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-global-themes-0-2/CONTEXT.md
@.planning/phases/07-global-themes-0-2/RESEARCH.md
@src/pipeline/query.ts
@src/pipeline/ids.ts
@src/pipeline/report.ts
@src/io/load-graph.ts
@src/types.ts
@src/index.ts
@tests/query.test.ts
@tests/report.test.ts
@package.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end detectCommunities on two-clique synthetic graph</name>
  <files>src/types.ts, src/pipeline/communities.ts, src/index.ts, tests/communities.test.ts</files>
  <behavior>
    - detectCommunities({ graph: twoCliques, write: false }) returns community_count semantics: communities.length === 2
    - Each community has size &gt;= 3; members partition {a*} vs {b*} (no cross-clique membership)
    - Community ids are c_0001, c_0002 (or equivalent padStart 4 after stable sort)
    - iterations is integer 1..20; stopped_reason is 'converged' or 'max_iterations'
    - Copyright header present on communities.ts
  </behavior>
  <action>
    Add Community, DetectCommunitiesOptions, and DetectCommunitiesResult interfaces to src/types.ts matching RESEARCH library API sketch (id, stable_key, label, members, size, internal_triple_count, top_predicates, top_nodes; options: dir?, graph?, maxIterations?, minSize?, write?; result: communities, iterations, stopped_reason, nodes_considered, edges_considered, dropped_small_count, optional paths).

    Create src/pipeline/communities.ts with copyright header per D-09. Export COMMUNITY_MAX_ITERATIONS = 20 and COMMUNITY_MIN_SIZE = 3 (D-02). Implement the full pure pipeline prescribed in RESEARCH Pattern 1:
    1. projectCommunityEdges(graph): undirected unique neighbors from triples where confidenceRank(t.confidence) &gt;= confidenceRank('INFERRED') only (D-03). Import confidenceRank from ./ids. Do not call buildAdjacencyMap unfiltered.
    2. labelPropagation(neighbors, maxIterations): init label[v]=v; each iter process nodes sorted ascending id (async updates); majority neighbor label; frequency ties → lexicographically smallest label (Claude discretion lock A1); stop when no changes or majority-stable or max iters.
    3. finalizeCommunities: group by label; BFS-split disconnected same-label components on undirected edges; drop groups with members.length &lt; minSize (D-02); sort size desc then min(memberId) asc; assign id `c_${padStart(4,'0')}` and stable_key = first 16 hex of sha256(membersSorted.join('\0')) via node:crypto; compute internal_triple_count, top_predicates, top_nodes (internal degree), deterministic theme label = top_nodes[0].label else `Community ${id}` (A3).
    4. detectCommunities(opts): when opts.graph provided, use it (tracer path); when write is false or omitted false in this plan, skip FS. Defaults maxIterations=20, minSize=3. Production loadGraphV1 path may stub-throw or defer full write to plan 07-02, but when graph is injected the happy path must return a complete DetectCommunitiesResult without network.

    Export detectCommunities, COMMUNITY_MAX_ITERATIONS, COMMUNITY_MIN_SIZE, and related types from src/index.ts.

    tests/communities.test.ts (node:test, offline, D-10): build minimal GraphV1Document in memory — Clique A three EXTRACTED mutual edges among a1,a2,a3; Clique B same for b1,b2,b3; schema_version 1, engine gsd-graph, required GraphV1 fields. Assert two communities, correct partition, ids present. No package installs.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run build:test &amp;&amp; node --test dist-test/communities.test.js</automated>
  </verify>
  <done>Two-clique synthetic graph yields two LP communities end-to-end via detectCommunities({ graph, write: false }) with public exports (COM-01, D-01, D-02, D-10).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confidence filter, min-size, max-iter, determinism expansion</name>
  <files>src/pipeline/communities.ts, tests/communities.test.ts</files>
  <behavior>
    - AMBIGUOUS-only bridge a1—b1 does not merge the two cliques into one community
    - A pair/group of size 2 is dropped; dropped_small_count increments
    - maxIterations: 0 or 1 still returns a defined stopped_reason; default cap is COMMUNITY_MAX_ITERATIONS
    - Two consecutive detectCommunities({ graph, write: false }) results deep-equal on communities (ids, members, stable_key)
    - INFERRED edges alone can form a community of size &gt;= 3 when EXTRACTED absent
  </behavior>
  <action>
    Expand tests and harden communities.ts so RESEARCH pitfalls are covered (D-03, D-02, D-05, D-10):
    - AMBIGUOUS bridge between cliques: still two communities.
    - Extra dyad (two nodes, one EXTRACTED edge): dropped_small_count &gt;= 1; not listed in communities.
    - Determinism: JSON-stable compare of communities arrays across two calls.
    - Export projectCommunityEdges and/or isCommunityEdge only if useful for unit isolation; otherwise keep private and test via detectCommunities.
    - Clamp maxIterations and minSize: non-finite or negative → defaults; maxIterations upper-bound at COMMUNITY_MAX_ITERATIONS (DoS guard T-07-02).
    - Do not mutate graph.v1.communities on the injected document; prefer sidecars only later (discretion A4 / D-04 spirit).
    - Do not add runtime dependencies; algorithm stays pure TypeScript (D-01).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run build:test &amp;&amp; node --test dist-test/communities.test.js</automated>
  </verify>
  <done>COM-01 core algorithm gates green: AMBIGUOUS exclusion, min-size drop, iteration cap, bit-stable partitions (D-01..D-03, D-05, D-10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Injected GraphV1Document → LPA | Trusted test/library caller; production will load via loadGraphV1 only |
| Algorithm parameters maxIterations/minSize | Untrusted numeric CLI/library inputs must be clamped |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | projectCommunityEdges | medium | mitigate | Rank gate EXTRACTED\|INFERRED only; never treat projection graph.json as input |
| T-07-02 | Denial of Service | labelPropagation | medium | mitigate | Cap iterations at COMMUNITY_MAX_ITERATIONS (20); clamp opts |
| T-07-03 | Information Disclosure | Community reports (later) | low | accept | Algorithm-only plan; no secrets in labels beyond graph content |
| T-07-SC | Tampering | npm installs | low | accept | No new packages this plan |
</threat_model>

<verification>
- dist-test/communities.test.js covers two-clique, AMBIGUOUS bridge, min-size, determinism
- npm run build succeeds with communities exports
</verification>

<success_criteria>
- Pure-TS LPA library callable with injected graph (COM-01 core)
- Defaults: max 20 iters, min size 3; undirected EXTRACTED|INFERRED edges only
- Offline node:test green for known community structure
</success_criteria>

<output>
Create `.planning/phases/07-global-themes-0-2/07-01-SUMMARY.md` when done
</output>
