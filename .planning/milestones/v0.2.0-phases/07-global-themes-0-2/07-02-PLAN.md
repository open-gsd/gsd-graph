---
phase: 07-global-themes-0-2
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/pipeline/communities.ts
  - src/types.ts
  - src/index.ts
  - tests/communities.test.ts
autonomous: true
requirements:
  - COM-01
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "detectCommunities without injected graph loads SoT only via loadGraphV1 (D-08); missing graph.v1 yields SCHEMA_INVALID"
    - "When write is true (default for production detect), store/communities/index.json and community-c_NNNN.md are written under confineUnderRoot (D-04)"
    - "Community markdown headers state non-authoritative theme report; source of truth remains graph.v1.json (D-04, D-05)"
    - "detectCommunities does not rewrite graph.v1.json bytes/semantic triple set (D-04)"
    - "writeCommunityReports rewrites markdown from last index.json or provided communities; fails clearly if index absent and no communities passed (D-05, A2)"
  artifacts:
    - path: src/pipeline/communities.ts
      provides: "FS write path, writeCommunityReports, COMMUNITIES_DIR"
    - path: tests/communities.test.ts
      provides: "temp-store loadGraphV1 + artifact + SoT-unchanged gates"
  key_links:
    - from: detectCommunities({ dir })
      to: loadGraphV1(storeRoot)
      via: "resolveStoreRoot then SoT only (D-08)"
    - from: write path
      to: confineUnderRoot(storeRoot, path.join('communities', fileName))
      via: "snapshot.ts pattern; basenames from c_NNNN only (D-04, T-07-04)"
    - from: community-*.md
      to: "non-authoritative header block"
      via: "RESEARCH deterministic report template (D-05)"
---

<objective>
Wire community detection to production store I/O: load via loadGraphV1, write disposable communities/index.json and community-*.md theme reports under realpath confinement, never promote communities to SoT (COM-01 artifacts; D-04, D-05, D-08, D-09, D-10).

Purpose: Operators need durable, disposable global-theme artifacts beside graph.v1 without corrupting the authority of triples.
Output: FS-backed detectCommunities + writeCommunityReports + integration tests on temp stores.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-global-themes-0-2/CONTEXT.md
@.planning/phases/07-global-themes-0-2/RESEARCH.md
@.planning/phases/07-global-themes-0-2/07-01-PLAN.md
@src/pipeline/communities.ts
@src/pipeline/snapshot.ts
@src/pipeline/report.ts
@src/io/load-graph.ts
@src/io/paths.ts
@src/io/atomic-publish.ts
@tests/communities.test.ts
@tests/report.test.ts
@tests/snapshot.test.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end detectCommunities writes communities/ from loadGraphV1 store</name>
  <files>src/pipeline/communities.ts, src/index.ts, tests/communities.test.ts</files>
  <behavior>
    - Publish a two-clique GraphV1Document into a temp store via publishGraphFiles (or equivalent); detectCommunities({ dir }) with default write loads via loadGraphV1 and returns index_path under .../communities/index.json
    - community-c_0001.md (and second community file) exist; report_paths length matches communities.length
    - index.json lists community ids, sizes, stable_keys, labels
    - After detect, loadGraphV1 still returns the same triple id set as before detect (SoT unchanged)
    - Missing store / missing graph.v1 → GraphError SCHEMA_INVALID (or existing reason from loadGraphV1)
  </behavior>
  <action>
    Extend detectCommunities production path (D-08, D-04):
    - When opts.graph is omitted: resolveStoreRoot from opts.dir, loadGraphV1(storeRoot) only — never open graph.json.
    - Default write to true when loading from store (library production); keep write:false for pure tests from 07-01.
    - Export COMMUNITIES_DIR = 'communities'. Create directory via confineUnderRoot(storeRoot, COMMUNITIES_DIR) + mkdirSync recursive (mirror snapshot.ts SNAP_DIR pattern).
    - Write index.json (machine summary: communities array with id, size, label, stable_key, members optional, algorithm meta max_iter/min_size/iterations/stopped_reason/nodes_considered/edges_considered/dropped_small_count) using confined path join('communities','index.json'). Prefer write-temp then rename if easy; sync write is acceptable if confined.
    - Write community-${id}.md only from assigned c_NNNN ids — never from raw label strings (path safety T-07-04).
    - Markdown body follows RESEARCH deterministic report template: H1 Community id — theme title; blockquote Non-authoritative theme report. Source of truth is graph.v1.json; Generated by gsd-graph label propagation (max_iter=…, min_size=…); members/internal_triples/stable_key; Top nodes; Top predicates; Members list. No LLM calls (D-05).
    - Return index_path and report_paths absolute paths on DetectCommunitiesResult.
    - Re-export write helpers / COMMUNITIES_DIR from index if public.

    Integration tests in communities.test.ts: temp dir, ensureStoreRoot, publishGraphFiles with two-clique document, detectCommunities({ dir }), assert files + SoT triple ids unchanged via loadGraphV1 before/after. Offline only (D-10).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run build:test &amp;&amp; node --test dist-test/communities.test.js</automated>
  </verify>
  <done>Store-backed detect writes communities/ sidecars from loadGraphV1 without mutating graph.v1 SoT (D-04, D-08, COM-01).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: writeCommunityReports rewrite path + index contract</name>
  <files>src/pipeline/communities.ts, src/types.ts, src/index.ts, tests/communities.test.ts</files>
  <behavior>
    - writeCommunityReports({ dir }) after a prior detect rewrites markdown files and returns index_path + report_paths
    - writeCommunityReports({ dir }) with no communities/index.json throws GraphError with a clear message (prefer SCHEMA_INVALID or USAGE-style reason already used in project)
    - writeCommunityReports({ communities, dir }) can write from in-memory Community[] without re-running LPA
    - index.json remains non-authoritative; graph.v1.communities field is not required to be populated (A4)
  </behavior>
  <action>
    Implement writeCommunityReports(opts?: { dir?: string; communities?: Community[] }) per RESEARCH (D-05, A2):
    - If communities provided: write index + markdown from that array under resolveStoreRoot(dir).
    - Else: read confined communities/index.json; if missing, throw GraphError with message that detect must run first (or equivalent clear reason).
    - Reuse the same markdown renderer as detect write path (single function).
    - Export writeCommunityReports and WriteCommunityReportsResult type from types or communities module via index.ts.
    - Tests: detect then report rewrite; delete markdown only and report regenerates; missing index fails without creating SoT mutations.
    - Confirm detect never assigns communities as SoT: optional assert that graph.v1.json file mtime/content hash of triples is stable across detect (D-04).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run build:test &amp;&amp; node --test dist-test/communities.test.js</automated>
  </verify>
  <done>writeCommunityReports + index rewrite path works; sidecars only; deterministic reports default (D-04, D-05, COM-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Store root → communities/ writes | All paths must stay under realpath-confined store |
| Community filenames | Derived only from generated c_NNNN ids |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Tampering | community-*.md paths | high | mitigate | Basenames only from c_NNNN; always confineUnderRoot |
| T-07-05 | Tampering | Treating reports as SoT | medium | mitigate | Non-authoritative header; tests prove graph.v1 triples unchanged |
| T-07-06 | Spoofing | Future LLM prose | low | accept | Deterministic-only this phase (D-05); no LLM path |
| T-07-SC | Tampering | npm installs | low | accept | No new packages |
</threat_model>

<verification>
- communities.test.js covers loadGraphV1 path, artifact writes, SoT stability, report rewrite, missing index error
</verification>

<success_criteria>
- Communities artifacts live under store/communities/ only
- loadGraphV1 is the sole graph load path for production detect
- Theme reports are deterministic and explicitly non-authoritative
</success_criteria>

<output>
Create `.planning/phases/07-global-themes-0-2/07-02-SUMMARY.md` when done
</output>
