---
phase: 07-global-themes-0-2
plan: 03
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - src/cli.ts
  - package.json
  - CHANGELOG.md
  - README.md
  - tests/cli-commands.test.ts
  - tests/package-identity.test.ts
autonomous: true
requirements:
  - COM-01
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "gsd-graph communities detect --dir <store> exits 0 and prints K22 JSON with ok true, community_count, communities summary, index_path (D-06, CLI-02 pattern)"
    - "gsd-graph communities report --dir <store> rewrites markdown from index and prints JSON paths; missing index maps to non-zero GraphError exit (D-06)"
    - "package.json version is 0.2.0; CHANGELOG has a 0.2.0 section documenting communities as the global-search differentiator (D-07)"
    - "README documents communities detect|report and positions global themes / 0.2.0 (D-07)"
  artifacts:
    - path: src/cli.ts
      provides: "nested communities detect|report commands"
    - path: package.json
      provides: "version 0.2.0"
    - path: CHANGELOG.md
      provides: "0.2.0 release notes for communities"
    - path: README.md
      provides: "CLI + product blurb for global themes"
    - path: tests/cli-commands.test.ts
      provides: "communities detect/report smoke"
  key_links:
    - from: "cli communities detect"
      to: detectCommunities({ dir, maxIterations?, minSize? })
      via: "thin adapter; writeOk JSON (D-06)"
    - from: "cli communities report"
      to: writeCommunityReports({ dir })
      via: "thin adapter; mapCliError exits (D-06)"
    - from: package.json version
      to: readEngineVersion in build.ts
      via: "engine_version stamp on new builds after bump (D-07)"
---

<objective>
Ship the operator surface and 0.2.0 identity: nested K22 CLI `communities detect|report`, package version bump, CHANGELOG, and README global-themes documentation (COM-01 ship; D-06, D-07, D-09, D-10).

Purpose: Make community detection invocable from the published binary and record 0.2.0 as the global-search milestone.
Output: CLI verbs, version 0.2.0, docs, CLI smoke tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-global-themes-0-2/CONTEXT.md
@.planning/phases/07-global-themes-0-2/RESEARCH.md
@.planning/phases/07-global-themes-0-2/07-01-PLAN.md
@.planning/phases/07-global-themes-0-2/07-02-PLAN.md
@src/cli.ts
@src/pipeline/communities.ts
@src/index.ts
@tests/cli-commands.test.ts
@tests/package-identity.test.ts
@package.json
@CHANGELOG.md
@README.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end CLI communities detect K22 JSON</name>
  <files>src/cli.ts, tests/cli-commands.test.ts</files>
  <behavior>
    - main(['node','gsd-graph','--dir',dir,'communities','detect']) returns exit 0 after a published two-clique store
    - stdout is single JSON object with ok: true, community_count &gt;= 1 (or 2 for two-clique), communities array with id/size/label/stable_key, index_path string
    - stdout is parseable JSON only (no human banner on stdout)
    - Unknown nested verb or missing detect still maps through mapCliError / commander exitOverride to non-zero
  </behavior>
  <action>
    In src/cli.ts, import detectCommunities (and writeCommunityReports when needed) from ./pipeline/communities. Add nested commander group matching snapshot/review style (D-06):

    program.command('communities').description('Detect communities and write theme reports (non-SoT)')

    communities.command('detect')
    - options: --min-size &lt;n&gt; (parseIntOpt), --max-iter &lt;n&gt; (parseIntOpt)
    - action: call detectCommunities(withDir({ minSize, maxIterations, write: true }, globalDir(cmd))); writeOk payload shaped like RESEARCH CLI result (ok: true, community_count, iterations, stopped_reason, nodes_considered, edges_considered, dropped_small_count, communities: [{id,size,label,stable_key}], index_path, report_paths).

    Do not register Louvain/Leiden or extra algorithms. Keep copyright header on cli.ts if editing top (already present).

    Extend tests/cli-commands.test.ts: temp store, publish two-clique graph.v1 (reuse publishGraphFiles / patterns from report or communities tests), run communities detect, assert exit 0 + JSON fields. Offline (D-10).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run build:test &amp;&amp; node --test dist-test/cli-commands.test.js</automated>
  </verify>
  <done>CLI communities detect returns K22 JSON success over library detectCommunities (D-06, COM-01).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: communities report CLI + 0.2.0 version, CHANGELOG, README</name>
  <files>src/cli.ts, package.json, CHANGELOG.md, README.md, tests/cli-commands.test.ts, tests/package-identity.test.ts</files>
  <behavior>
    - communities report after detect exits 0 with index_path and report_paths in JSON
    - communities report without prior detect exits non-zero (GraphError path → 2)
    - package.json "version" equals "0.2.0"
    - CHANGELOG.md contains a ## [0.2.0] section mentioning community detection / label propagation / global themes
    - README documents gsd-graph communities detect and communities report; notes communities/ are not SoT
  </behavior>
  <action>
    Wire communities.command('report') calling writeCommunityReports(withDir({}, globalDir(cmd))); writeOk({ ok: true, ...paths }).

    Bump package.json version from 0.1.0 to 0.2.0 (D-07). readEngineVersion already reads package.json so new builds stamp 0.2.0 — no extra code unless tests assert old version.

    Prepend CHANGELOG.md section [0.2.0] - 2026-08-03 (or current date) under Added: pure-TS label-propagation communities, communities/ theme reports (non-SoT), CLI communities detect|report, global-search differentiator note (D-07). Keep prior [0.1.0] history. Update deferred notes that said communities deferred.

    Update README.md: CLI table/examples for communities detect|report; short product pitch that 0.2.0 adds corpus-level global themes via communities (vs local pack/answer); honesty that community markdown is disposable not SoT. Do not claim network/LLM community essays.

    Tests: cli-commands report happy + missing-index exit; package-identity or small assert package.json version is 0.2.0 (add test if none exists). Full npm test green.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>0.2.0 ship surface complete: CLI report verb, version, CHANGELOG, README global themes (D-06, D-07, COM-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → library | Untrusted flags; numeric parse + library clamps |
| stdout JSON | Machine contract only; no secrets beyond local store paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07 | Information Disclosure | CLI stdout paths | low | accept | Local absolute paths expected for operators; no credentials |
| T-07-08 | Elevation of Privilege | CLI write communities | low | mitigate | Same process trust as other store writers; confined paths from library |
| T-07-SC | Tampering | npm installs | low | accept | No new packages; version bump only |
</threat_model>

<verification>
- cli-commands communities detect/report green
- package.json version 0.2.0
- npm test full suite green
</verification>

<success_criteria>
- Nested communities CLI works under K22
- Package identity is 0.2.0 with CHANGELOG + README documenting communities as global-search differentiator
- COM-01 phase ship criteria met with 07-01..07-03
</success_criteria>

<output>
Create `.planning/phases/07-global-themes-0-2/07-03-SUMMARY.md` when done
</output>
