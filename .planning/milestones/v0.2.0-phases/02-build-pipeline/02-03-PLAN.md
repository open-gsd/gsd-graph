---
phase: 02-build-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/types.ts
  - src/pipeline/normalize.ts
  - src/pipeline/review.ts
  - src/schema/validators.ts
  - src/index.ts
  - schemas/review-queue.schema.json
  - tests/normalize.test.ts
  - tests/review-queue.test.ts
autonomous: true
requirements:
  - NORM-01
  - NORM-02
  - REV-01
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Dedup on (s,p,o) unions provenance multisets; triple.confidence = bestTier(entries) (D-05, NORM-01)"
    - "Auto-merge only exact same-type id or exact normalized alias; cross-type never auto-merges (D-06, NORM-02)"
    - "same_as triples do not rewrite node ids until entity_merge accept (D-06)"
    - "Unknown type/predicate with policy review emits review item and does not write the contested triple (D-07)"
    - "Review item ids are stable rv_[0-9a-f]{8} across rebuilds for the same kind+canonical payload (D-08, REV-01)"
    - "accept mutates graph/ontology per kind; reject records decision without writing contested draft (D-08, REV-01)"
  artifacts:
    - path: src/pipeline/normalize.ts
      provides: "normalize() merge + best_tier + policy gate"
    - path: src/pipeline/review.ts
      provides: "load/save review-queue + reviewResolve accept/reject"
    - path: schemas/review-queue.schema.json
      provides: "Ajv authority for review queue SoT"
    - path: tests/normalize.test.ts
      provides: "NORM-01/02 + policy integration"
    - path: tests/review-queue.test.ts
      provides: "REV-01 stable ids + accept/reject effects"
  key_links:
    - from: normalize
      to: applyUnknownPolicy
      via: "review|coerce|drop before triple write (D-07)"
    - from: reviewItemId
      to: review-queue.json items[].id
      via: "kind + stableStringify(payload) hash (D-08)"
    - from: reviewResolve accept entity_merge
      to: graph.v1 nodes/triples
      via: "rewrite s/o drop→keep under lock + publish (D-08, D-09)"
---

<objective>
Implement normalize (multiset provenance, best_tier, exact same-type merge) and the review queue control plane with stable ids and accept/reject effects.

Purpose: NORM-01/02 and REV-01 prevent false merges and silent ontology expansion while preserving EXTRACTED evidence for incremental maintain later.
Output: normalize, review-queue schema + validator, reviewResolve, unit tests, exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-build-pipeline/CONTEXT.md
@.planning/phases/02-build-pipeline/RESEARCH.md
@.planning/phases/02-build-pipeline/02-01-SUMMARY.md
@.planning/phases/02-build-pipeline/02-02-SUMMARY.md
@src/ontology/policy.ts
@src/ontology/load-pack.ts
@src/pipeline/ids.ts
@src/io/atomic-publish.ts
@src/io/lock.ts
@src/io/load-graph.ts
@src/types.ts
@docs/DESIGN.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end normalize one EXTRACTED triple with best_tier</name>
  <files>src/pipeline/normalize.ts, src/types.ts, src/index.ts, tests/normalize.test.ts</files>
  <behavior>
    - normalize({ ontology, nodes, triples }) returns { nodes, triples, reviewItems, diagnostics } (D-05)
    - Two input triples with same (s,p,o) and different provenance entries → one output triple with multiset union length 2 and confidence = bestTier (EXTRACTED wins over INFERRED) (NORM-01, D-05)
    - Known allowlisted predicate/type from general pack writes through (applyUnknownPolicy allow)
    - Unknown predicate with default general pack policy review → reviewItems entry kind predicate_unknown, triple not in output triples (D-07)
  </behavior>
  <action>
    Implement normalize tracer (D-05, D-07, D-11, D-12). Reuse loadOntologyPack + applyUnknownPolicy — do not reimplement policy matrix.

    1. Types in src/types.ts (or pipeline-local types re-exported): NormalizeInput, NormalizeOutput, ReviewKind, ReviewItem (pending shape), ReviewQueueDocument skeleton matching RESEARCH.

    2. src/pipeline/normalize.ts — normalize(input):
       - Canonicalize node ids via nodeId when id missing; keep explicit ids
       - For each triple: policy-check predicate; policy-check endpoint types if present on nodes
       - review → push ReviewItem via reviewItemId('predicate_unknown'|'type_unknown', stable payload without timestamps); do not write triple
       - coerce → rewrite p→related_to or type→Concept then write
       - drop → discard
       - Dedup key `${s}\0${p}\0${o}`: union provenance entries unique by source_path+extractor+content_hash+confidence; set confidence = bestTier(provenance); id = tripleId(s,p,o)

    3. tests/normalize.test.ts: construct in-memory nodes/triples (no FS); load general pack; assert multiset + best_tier; assert unknown predicate queues review.

    4. Export normalize + types from src/index.ts.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>normalize unions provenance and best_tiers; review policy gates unknown predicates (NORM-01, D-05, D-07).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exact same-type merge and same_as advisory</name>
  <files>src/pipeline/normalize.ts, tests/normalize.test.ts</files>
  <behavior>
    - Two nodes same type with identical id → single keeper; aliases/labels merged (D-06, NORM-02)
    - Two nodes same type where one's label/alias slug-equals the other's id local part → exact alias merge into keeper
    - Cross-type same label → no auto-merge; emit entity_merge review item (pending) when conflict is ambiguous (D-06)
    - Triple with p=same_as may remain as advisory edge when predicate allowed; node ids are not rewritten because of same_as alone (D-06, NORM-02)
    - Collision on canonical slug for distinct keepers uses numeric suffixes -2, -3 only when forced unique ids required — document behavior in code comment
  </behavior>
  <action>
    Expand normalize identity logic per D-06 / K23 / RESEARCH Pattern normalize:

    1. Build index by id; scan for exact same-type alias matches (normalized slug equality on label or aliases array entries).
    2. Merge: keep first-seen keeper id; union aliases; prefer non-empty description; rewrite any triples s/o from dropped id → keeper before dedup.
    3. Non-exact / cross-type clashes → reviewItems kind entity_merge with payload { keep, drop, reason } using reviewItemId; do not merge.
    4. After merges, run triple policy + multiset dedup as in Task 1.
    5. Tests covering exact merge node-count drop, cross-type no merge, same_as edge leaves both nodes.

    Do not implement fuzzy/Levenshtein matching (forbidden D-06).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Exact same-type merge only; same_as advisory; cross-type conflicts queue (NORM-02, D-06).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Review queue schema, stable rv_ ids, accept/reject resolve</name>
  <files>schemas/review-queue.schema.json, src/schema/validators.ts, src/pipeline/review.ts, src/types.ts, src/index.ts, tests/review-queue.test.ts</files>
  <behavior>
    - schemas/review-queue.schema.json validates ReviewQueueDocument: schema_version 1, items[], decisions[] (OQ-4, D-08, REV-01)
    - validateReviewQueue compile-once Ajv like validateGraphV1
    - Same kind+payload → identical rv_ id across two normalize runs (stable; no created_at in hash input) (D-08)
    - reviewResolve({ storeRoot, id, action: 'reject' }) sets item status rejected, appends decisions[], does not add contested triple to graph (D-08)
    - reviewResolve accept entity_merge rewrites triples and deletes drop node then publishGraphFiles under acquireBuildLock (D-08, D-09)
    - reviewResolve accept predicate_unknown with extendOntology true may add predicate to ontology.lock.json sidecar and write triple; without extendOntology, accept must coerce to related_to or fail closed without ambient lock expand (D-08, D-07)
    - Prior decision for same id prevents re-opening pending on identical payload rebuild (retain decisions[]) (Pitfall 3)
  </behavior>
  <action>
    Implement review control plane (D-08, D-09, D-11, D-12, OQ-4).

    1. schemas/review-queue.schema.json — draft-2020-12, additionalProperties false on items; id pattern ^rv_[0-9a-f]{8}$; status enum pending|accepted|rejected; kind enum entity_merge|predicate_unknown|type_unknown|schema_drift.

    2. src/schema/validators.ts — load review-queue.schema.json; export validateReviewQueue.

    3. src/pipeline/review.ts:
       - loadReviewQueue(storeRoot) / save via publish path helpers (read JSON with validate; empty default document if missing)
       - mergeReviewItems(existing, incoming): by id; do not reset accepted/rejected to pending when decisions contain id
       - reviewResolve(opts): acquireBuildLock(store, 'lib'); try loadGraphV1 + queue; apply accept/reject effects from RESEARCH table; publishGraphFiles with updated graphV1 + review-queue.json sidecar; finally release
       - Effects table must match RESEARCH accept effects for entity_merge, predicate_unknown, type_unknown; schema_drift accept is record-only

    4. tests/review-queue.test.ts: temp store; seed graph + queue; stable id assertion; reject no-write; accept entity_merge merges; optional extendOntology path tested with flag true/false.

    5. Export reviewResolve, loadReviewQueue (if public), validateReviewQueue, Review* types from src/index.ts.

    Prefer calling Phase 1 IO; do not hand-roll dual-write.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Review queue schema + stable rv_ + accept/reject mutate only on accept (REV-01, D-08, D-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extract candidates → normalize allowlist | Untrusted types/predicates cross ontology boundary |
| reviewResolve accept → graph.v1 / ontology.lock | Privileged mutation path (library API; no ambient auto-accept) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering | normalize identity merge | high | mitigate | Exact same-type only; no fuzzy merge (D-06) |
| T-02-07 | Tampering | unknown predicate/type | high | mitigate | applyUnknownPolicy review default; no write (D-07) |
| T-02-08 | Elevation of privilege | reviewResolve extendOntology | medium | mitigate | extend only when opts.extendOntology true; never ambient |
| T-02-09 | Tampering | concurrent reviewResolve | high | mitigate | acquireBuildLock around mutate+publish (D-09) |
| T-02-SC | Tampering | npm installs | high | mitigate | No new packages; reuse ajv |
</threat_model>

<verification>
- npm test: normalize + review-queue green
- schemas/review-queue.schema.json present and compiled
- accept/reject effects covered
</verification>

<success_criteria>
NORM-01, NORM-02, REV-01 satisfied with stable review ids and best_tier multiset provenance (D-05, D-06, D-07, D-08).
</success_criteria>

<output>
Create `.planning/phases/02-build-pipeline/02-03-SUMMARY.md` when done
</output>
