---
phase: 02-build-pipeline
plan: 04
type: execute
wave: 4
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - src/types.ts
  - src/pipeline/build.ts
  - src/pipeline/status.ts
  - src/index.ts
  - tests/build-pipeline.test.ts
  - tests/status.test.ts
autonomous: true
requirements:
  - EXT-03
  - STAT-01
  - EXT-01
  - EXT-02
  - NORM-01
  - NORM-02
  - REV-01
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "build(opts) under acquireBuildLock discovers corpus, extracts, normalizes, publishes graph.v1.json via publishGraphFiles, releases lock (D-09)"
    - "sources.manifest.json records path → content_hash; rebuild with full:false skips unchanged fingerprints (D-04, EXT-03)"
    - "status() reports exists, node_count, triple_count, engine gsd-graph, last_build/freshness without reading graph.json as SoT (D-10, STAT-01)"
    - "Hard caps: nodes>100000 or triples>250000 throw LIMIT_EXCEEDED; missing corpus CORPUS_NOT_FOUND"
    - "build writes review-queue.json and ontology.lock.json sidecars alongside graph.v1"
  artifacts:
    - path: src/pipeline/build.ts
      provides: "build() orchestrator stages under lock"
    - path: src/pipeline/status.ts
      provides: "status() read path over v1 + lock + queue + status file"
    - path: tests/build-pipeline.test.ts
      provides: "lock+publish+manifest+incremental integration"
    - path: tests/status.test.ts
      provides: "STAT-01 field gates"
  key_links:
    - from: build
      to: publishGraphFiles
      via: "caller-held acquireBuildLock → publish → release (D-09)"
    - from: build
      to: sources.manifest.json
      via: "sidecar content_hash per source (D-04, EXT-03)"
    - from: status
      to: loadGraphV1
      via: "never open graph.json as truth (D-09, D-10)"
---

<objective>
Wire the full offline build orchestrator and status read path so a corpus becomes a durable graph.v1 with honest counts, engine identity, fingerprints, and review sidecars.

Purpose: Close Phase 2 success criteria by composing extract → normalize → review queue → publish → status (STAT-01, EXT-03 incremental, D-09/D-10).
Output: build(), status(), integration tests, public exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-build-pipeline/CONTEXT.md
@.planning/phases/02-build-pipeline/RESEARCH.md
@.planning/phases/02-build-pipeline/02-01-SUMMARY.md
@.planning/phases/02-build-pipeline/02-02-SUMMARY.md
@.planning/phases/02-build-pipeline/02-03-SUMMARY.md
@src/pipeline/normalize.ts
@src/pipeline/review.ts
@src/sources/discover.ts
@src/pipeline/extract.ts
@src/io/atomic-publish.ts
@src/io/lock.ts
@src/io/load-graph.ts
@src/index.ts
@package.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end build one structured MD file to graph.v1 + status</name>
  <files>src/pipeline/build.ts, src/pipeline/status.ts, src/types.ts, src/index.ts, tests/build-pipeline.test.ts, tests/status.test.ts</files>
  <behavior>
    - build({ corpus, dir }) with structured-edges.md (or temp copy) creates store, acquires lock, publishes graph.v1.json loadable via loadGraphV1 (D-09)
    - BuildResult includes node_count, triple_count, review_pending, engine: 'gsd-graph', engine_version from package.json, built_at ISO, store_dir (D-10)
    - status({ dir }) after build: exists true; engine gsd-graph; node_count and triple_count match graph; last_build or last_build_status.finished_at set (STAT-01, D-10)
    - status never requires graph.json; projection absence is fine (D-09)
    - Lock released after build (second acquireBuildLock succeeds)
  </behavior>
  <action>
    Implement build + status tracer composing Phase 1 IO and Phase 2 stages (D-09, D-10, D-11, D-12, OQ-2 both stages + orchestrator).

    1. src/types.ts — BuildOptions, BuildResult, StatusResult, SourcesManifest types per RESEARCH Code Examples (engine literal 'gsd-graph').

    2. src/pipeline/build.ts — build(opts: BuildOptions): BuildResult
       - ensureStoreRoot(resolveStoreRoot({ dir: opts.dir }))
       - lock = acquireBuildLock(storeRoot, 'lib'); try/finally release
       - ontology = loadOntologyPack({ packIdOrPath: opts.ontology ?? 'general' })
       - discoverSources(opts.corpus); for each file extractByPath → concatenate nodes/triples/diagnostics
       - normalize({ ontology, nodes, triples })
       - Assemble GraphV1Document: schema_version 1, engine gsd-graph, engine_version from package.json, ontology ids/versions from pack, built_at now, nodes, triples, stats counts
       - validateGraphV1 before publish (publish also validates)
       - publishGraphFiles({ storeRoot, graphV1, writeProjection: opts.writeProjection ?? DEFAULT_WRITE_PROJECTION, sidecars: { 'sources.manifest.json', 'review-queue.json', 'ontology.lock.json' } })
       - Return BuildResult

    3. src/pipeline/status.ts — status(opts?: { dir?: string }): StatusResult
       - resolve store; if no graph.v1 → { exists:false, store_dir, engine:'gsd-graph' }
       - else loadGraphV1 (never projection)
       - best-effort read .last-build-status.json, review-queue.json pending count, .build.lock presence → build_in_progress
       - Fill STAT-01 fields: node_count, triple_count, edge_count===triple_count, ontology_pack_id, last_build from built_at, engine identity

    4. tests/build-pipeline.test.ts + tests/status.test.ts: temp dirs; copy or inline structured fixture; assert loadGraphV1 after build; assert status counts; assert lock released.

    5. Export build, status, BuildOptions, BuildResult, StatusResult from src/index.ts.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>One-file build publishes graph.v1 under lock; status reports counts and engine (D-09, D-10, STAT-01 tracer).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Incremental fingerprints, sidecars, caps, multi-source build</name>
  <files>src/pipeline/build.ts, tests/build-pipeline.test.ts</files>
  <behavior>
    - sources.manifest.json schema_version 1 with sources map: content_hash, mtime_ms, bytes, last_extracted_at, extractor (OQ-3, D-04, EXT-03)
    - Second build with full:false and unchanged bytes skips re-extract for those paths; sources_skipped_fresh ≥ 1; graph still valid (D-04, EXT-03)
    - full:true forces re-extract all
    - JSONL multi-hop fixture alone builds EXTRACTED chain into graph.v1 (wires EXT-02)
    - Combined MD + JSONL corpus merges via normalize multiset (wires NORM-01)
    - nodes greater than 100_000 or triples greater than 250_000 → GraphError LIMIT_EXCEEDED before publish
    - Missing corpus root → CORPUS_NOT_FOUND
    - review-queue.json written even when empty items; ontology.lock.json includes pack id, version, packHash, types, predicates
    - Prior pending review items with existing decisions are not re-opened as pending (merge with review helpers from 02-03)
  </behavior>
  <action>
    Complete build orchestrator production path (D-04, D-09, EXT-03, caps from DESIGN).

    1. Incremental algorithm (Phase 2 scope — not full M1–M5):
       - Load prior graph.v1 if present + prior manifest
       - For each discovered file: fingerprint; if !opts.full and manifest hash matches, skip extract and retain prior triples/nodes whose provenance source_path matches that file (best-effort: keep all prior graph and only re-extract changed/new files then re-normalize union — document chosen strategy in code comment; must make sources_skipped_fresh observable)
       - Always re-normalize the working set so best_tier and merge stay correct

    2. Caps: after normalize, if nodes.length greater than 100000 or triples.length greater than 250000 throw GraphError(LIMIT_EXCEEDED).

    3. Sidecar payloads:
       - sources.manifest.json as OQ-3
       - review-queue.json via ReviewQueueDocument (mergeReviewItems)
       - ontology.lock.json: { pack_id, version, packHash, node_types, predicates: id[] } from LoadedOntology

    4. Tests: incremental skip; multi-hop.jsonl build; LIMIT_EXCEEDED via injecting huge arrays through a test-only hook OR unit-test the cap function if extracted; CORPUS_NOT_FOUND; sidecars exist after build.

    Do not implement CLI binary (Phase 4) or Query IR (Phase 3).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Incremental fingerprint skip, caps, multi-source publish, sidecars complete (EXT-03, D-04, D-09).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Status freshness, public façade completeness, coverage gate</name>
  <files>src/pipeline/status.ts, src/index.ts, tests/status.test.ts, tests/build-pipeline.test.ts</files>
  <behavior>
    - status on empty store: exists false; no throw
    - status after build: review_queue_count reflects pending items when unknown predicates queued
    - build_in_progress true when .build.lock held (simulate held lock file)
    - stale signal: when status is called with corpus option or when manifest entries' content_hash can be checked — Phase 2 minimum: age_hours from built_at and/or stale true if a manifest path is missing on disk when re-checked; document fields set
    - projection_stale may be true when writeProjection false and graph.json missing/absent — optional; do not treat projection as SoT
    - index.ts exports full Phase 2 surface: discoverSources, fingerprintFile, extractMarkdown, extractJsonl, extractByPath, normalize, reviewResolve, build, status, bestTier, id helpers, validateReviewQueue
    - npm run test:coverage lines ≥80 still holds
  </behavior>
  <action>
    Polish status() STAT-01 composition (D-10) and export surface (OQ-2).

    1. status reads:
       - loadGraphV1 when present
       - .last-build-status.json best-effort (compose; do not break minimal writer from publishGraphFiles)
       - review-queue pending count
       - lock file existence → build_in_progress (stale lock may still count as in progress if file exists — match Phase 1 STALE_MS optional)
       - optional freshness: if manifest exists and files missing → stale true

    2. Ensure public façade complete; package-identity tests still pass (no gsd-core dep).

    3. Integration smoke: free-prose corpus build produces graph without typed causes chain (honesty); structured or JSONL produces triples.

    4. Run npm test and npm run test:coverage; fix coverage gaps on new pipeline modules.

    Copyright headers on all new files (D-11).
  </action>
  <verify>
    <automated>npm run test:coverage</automated>
  </verify>
  <done>STAT-01 complete; Phase 2 public API exported; coverage ≥80; ROADMAP Phase 2 success criteria 1–5 achievable (D-09, D-10, D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus roots → build() | Untrusted paths enter discovery/extract/publish |
| build publish → store files | Multi-file write must stay locked and v1-first |
| status readers → store | Must not elevate projection to SoT |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Tampering | build publish | high | mitigate | acquireBuildLock + publishGraphFiles v1-first (D-09) |
| T-02-11 | Tampering | status SoT confusion | high | mitigate | loadGraphV1 only; never graph.json (D-09, D-10) |
| T-02-12 | Denial of service | huge corpus | medium | mitigate | 8 MiB skip; 100k/250k LIMIT_EXCEEDED |
| T-02-13 | Tampering | path escape corpus | high | mitigate | discoverSources realpath confine (T-02-01) |
| T-02-SC | Tampering | npm installs | high | mitigate | No new packages in Phase 2 |
</threat_model>

<verification>
- npm test && npm run test:coverage
- build from structured MD and multi-hop JSONL
- status after build reports counts + engine gsd-graph
- incremental rebuild skips fresh fingerprints
</verification>

<success_criteria>
All Phase 2 ROADMAP success criteria true: extract MD/JSONL, fingerprints + best_tier, exact merge, review accept/reject, status after offline build (EXT-01..03, NORM-01..02, REV-01, STAT-01).
</success_criteria>

<output>
Create `.planning/phases/02-build-pipeline/02-04-SUMMARY.md` when done
</output>
