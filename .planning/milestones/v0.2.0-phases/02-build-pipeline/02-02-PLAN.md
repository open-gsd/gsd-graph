---
phase: 02-build-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/sources/jsonl.ts
  - src/pipeline/extract.ts
  - src/index.ts
  - tests/fixtures/corpus/multi-hop.jsonl
  - tests/extract-jsonl.test.ts
autonomous: true
requirements:
  - EXT-02
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "JSONL field-map records emit EXTRACTED nodes and edge triples for multi-hop chains (D-03, EXT-02)"
    - "JSON array files and line-delimited JSONL both parse into ExtractResult"
    - "extractByPath (or extract orchestrator) routes .md/.txt → extractMarkdown and .json/.jsonl → extractJsonl using fingerprint content_hash"
    - "multi-hop.jsonl fixture seeds a typed EXTRACTED path usable by later goldens"
  artifacts:
    - path: src/sources/jsonl.ts
      provides: "extractJsonl field-map adapter"
    - path: src/pipeline/extract.ts
      provides: "per-file extract routing by extension"
    - path: tests/fixtures/corpus/multi-hop.jsonl
      provides: "EXTRACTED multi-hop chain seed"
    - path: tests/extract-jsonl.test.ts
      provides: "EXT-02 unit gates"
  key_links:
    - from: extractJsonl edges[].p
      to: Triple.p
      via: "field map p/o → EXTRACTED triple; policy deferred to normalize"
    - from: extractByPath
      to: fingerprintFile
      via: "content_hash stamped on every provenance entry (D-04)"
---

<objective>
Implement JSON/JSONL structured field-map extraction and a thin per-file extract router so multi-hop fixtures produce EXTRACTED triples offline.

Purpose: EXT-02 is the preferred path for honest multi-hop goldens (Phase 5 G1+); Phase 2 seeds the adapter and fixture.
Output: extractJsonl, extract orchestrator, multi-hop.jsonl fixture, tests, exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-build-pipeline/CONTEXT.md
@.planning/phases/02-build-pipeline/RESEARCH.md
@.planning/phases/02-build-pipeline/02-01-SUMMARY.md
@src/sources/markdown.ts
@src/sources/fingerprint.ts
@src/pipeline/ids.ts
@src/types.ts
@src/index.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end JSONL two-hop EXTRACTED chain</name>
  <files>src/sources/jsonl.ts, src/index.ts, tests/fixtures/corpus/multi-hop.jsonl, tests/extract-jsonl.test.ts</files>
  <behavior>
    - extractJsonl(sourcePath, content, contentHash) parses one-object-per-line JSONL (D-03, EXT-02)
    - Record shape: optional id; required type + label; optional aliases[]; optional edges[] of { p, o } where o is string id or { id?|type|label }
    - Two-hop fixture A -p1-> B -p2-> C yields three nodes and two EXTRACTED triples with provenance.extractor `jsonl/field-map` and content_hash bound (D-03, D-04)
    - triple ids are tripleId(s,p,o); confidence EXTRACTED on all field-map edges
    - Invalid JSON line yields diagnostic and continues (does not throw whole file unless content is empty/unreadable)
  </behavior>
  <action>
    Implement extractJsonl per RESEARCH field-map contract (D-03, D-11, D-12). Offline only (D-01).

    1. src/sources/jsonl.ts — extractJsonl(sourcePath, content, contentHash): ExtractResult
       - Detect JSON array (trim start with `[`) vs JSONL (line loop)
       - For each record: resolve node id = record.id ?? nodeId(type, label); push GraphNode with aliases
       - For each edge: resolve object id (string or nested type/label/id); emit Triple with p, EXTRACTED, provenance single entry
       - Apply redactSecrets from src/sources/redact.ts to labels before emission
       - Skip records missing type or label with diagnostic code RECORD_INVALID

    2. tests/fixtures/corpus/multi-hop.jsonl: at least three Concept (or Claim/Event) records forming a chain with allowlisted general-pack predicates (e.g. causes or related_to / about) so normalize later can write without forced review. Prefer predicates in general pack: causes between Concepts is allowlisted.

    3. tests/extract-jsonl.test.ts: load fixture; assert node count ≥3; assert path A→B→C via triple s/p/o; assert all triple.confidence === 'EXTRACTED'; assert provenance content_hash.

    4. Export extractJsonl from src/index.ts.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>JSONL field-map emits EXTRACTED multi-hop chain from fixture (EXT-02, D-03).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extractByPath router + JSON array file support</name>
  <files>src/pipeline/extract.ts, src/sources/jsonl.ts, src/index.ts, tests/extract-jsonl.test.ts</files>
  <behavior>
    - extractByPath(absPath, opts?: { contentHash?: string }): reads file, fingerprints if hash omitted, routes by extension:
      - .md .markdown .txt → extractMarkdown
      - .json .jsonl → extractJsonl
      - other → empty result + diagnostic UNSUPPORTED_EXTENSION
    - JSON array file (pretty-printed array of records) produces same nodes/triples as equivalent JSONL
    - Router never fetches URLs or invokes LLM (D-01)
  </behavior>
  <action>
    1. src/pipeline/extract.ts — extractByPath (name may be extractSource; export one clear name):
       - fingerprintFile(absPath) unless contentHash provided
       - read UTF-8 text; call appropriate extractor
       - Return ExtractResult

    2. Ensure extractJsonl handles JSON.parse of full array documents (not only line mode).

    3. Tests: temp .json array with two linked records; extractByPath on multi-hop.jsonl fixture matches direct extractJsonl; unsupported extension diagnostic.

    4. Export extractByPath (and extractJsonl if not already) from src/index.ts.

    Do not implement normalize or build.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Extension router + JSON array support green; EXT-02 complete (D-03, D-01, D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSONL record fields → graph nodes/triples | Untrusted structured corpus becomes typed graph candidates |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | src/sources/jsonl.ts | medium | mitigate | Field-map only; unknown types/predicates deferred to normalize policy (D-07); Ajv on publish later |
| T-02-05 | Information disclosure | labels in JSONL | medium | mitigate | redactSecrets on labels |
| T-02-SC | Tampering | npm installs | high | mitigate | No new packages |
</threat_model>

<verification>
- npm test includes extract-jsonl
- multi-hop.jsonl fixture present
- extractByPath routes MD and JSONL
</verification>

<success_criteria>
EXT-02 satisfied: JSON/JSONL field map yields EXTRACTED triples for multi-hop fixtures (D-03).
</success_criteria>

<output>
Create `.planning/phases/02-build-pipeline/02-02-SUMMARY.md` when done
</output>
