---
phase: 03-query-lifecycle-maintain
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/pipeline/maintain.ts
  - src/pipeline/project.ts
  - src/pipeline/build.ts
  - src/types.ts
  - src/index.ts
  - tests/maintain.test.ts
  - tests/build-pipeline.test.ts
autonomous: true
requirements:
  - MNT-01
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "invalidateProvenance drops provenance for pathsToDrop, recomputes bestTier, and removes triples with empty provenance (MNT-01, D-05, D-06)"
    - "M1–M5 matrix unit tests pass against pure invalidateProvenance / normalize (MNT-01, D-05, D-12)"
    - "build({ full: false }) invalidates changed ∪ removed sources even when zero files re-extract (deleted-source gap fix, D-06)"
    - "maintain(opts) is an alias of build({ ...opts, full: false }) with no second orchestrator (RESEARCH OQ-1)"
    - "Successful build writes snapshots/.last-diff-base.json as a full graph.v1 copy under lock (DIFF-01 prep, RESEARCH OQ-3)"
    - "When writeProjection is true, build passes projectGraph(v1) into publishGraphFiles so graph.json materializes (RESEARCH Pitfall 7)"
  artifacts:
    - path: src/pipeline/maintain.ts
      provides: "invalidateProvenance pure helper + maintain() alias"
    - path: src/pipeline/project.ts
      provides: "projectGraph(v1) disposable edges projection"
    - path: src/pipeline/build.ts
      provides: "full:false uses changed∪removed invalidation; last-diff-base; projection payload"
    - path: tests/maintain.test.ts
      provides: "M1–M5 + deleted-source integration"
  key_links:
    - from: build full:false
      to: invalidateProvenance
      via: "pathsToDrop = changedPaths ∪ removedManifestPaths (D-06)"
    - from: build success
      to: snapshots/.last-diff-base.json
      via: "write under same lock after publish (OQ-3)"
    - from: writeProjection true
      to: projectGraph
      via: "publishGraphFiles projection payload required"
---

<objective>
Close MNT-01: pure multiset provenance invalidation for M1–M5, wire deleted-source fix into build({ full: false }), optional maintain alias, last-diff-base snapshot baseline, and projection payload for writeProjection.

Purpose: Phase 2 incremental is incomplete — deleted corpus sources leave stale EXTRACTED triples. Normative maintain path is build({ full: false }) + invalidateProvenance (user decision / RESEARCH OQ-1).
Output: maintain.ts, project.ts, build wiring, M1–M5 tests, public exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-query-lifecycle-maintain/CONTEXT.md
@.planning/phases/03-query-lifecycle-maintain/RESEARCH.md
@.planning/phases/02-build-pipeline/02-04-SUMMARY.md
@docs/DESIGN.md
@src/pipeline/build.ts
@src/pipeline/ids.ts
@src/pipeline/normalize.ts
@src/io/atomic-publish.ts
@src/io/lock.ts
@src/io/paths.ts
@src/types.ts
@src/index.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end invalidateProvenance M1 + deleted-source build path</name>
  <files>src/pipeline/maintain.ts, src/pipeline/build.ts, src/index.ts, tests/maintain.test.ts</files>
  <behavior>
    - invalidateProvenance(triples, pathsToDrop): drops provenance entries whose source_path normalizes into pathsToDrop; recomputes confidence via bestTier; omits triples with empty provenance (D-05, D-06, MNT-01)
    - M1 unit: two provenance entries EXTRACTED+INFERRED; drop EXTRACTED path → triple remains with confidence INFERRED (DESIGN M1)
    - Integration: full build two corpus files; delete one file from disk; build({ full: false }) removes triples that only had provenance from the deleted path (deleted-source gap, D-06)
    - When pathsToDrop empty, returns cloned triples without mutation of inputs
  </behavior>
  <action>
    Extract pure invalidation and prove the deleted-source fix on the incremental build path (per D-05, D-06, D-10 lock already in build, D-11, D-12; RESEARCH OQ-1 RESOLVED).

    1. Create src/pipeline/maintain.ts:
       - normPathKey helper consistent with build.ts (realpath when possible)
       - invalidateProvenance(triples, pathsToDrop: ReadonlySet&lt;string&gt;): Triple[] implementing strip logic currently private as stripChangedSources in build.ts
       - Export maintain(opts: BuildOptions): BuildResult as maintain(opts) { return build({ ...opts, full: false }); } — import build carefully to avoid circular init issues (if cycle: define maintain in build.ts re-export, or lazy require). Prefer maintain.ts importing build if Node CJS cycle is safe; otherwise export maintain from build.ts and re-export from maintain.ts for API surface.

    2. Replace build.ts stripChangedSources usage:
       - Compute removedPaths = keys present in priorManifest.sources but not in current discovered set (normalize both sides with normPathKey)
       - pathsToDrop = changedPaths ∪ removedPaths
       - When !full &amp;&amp; priorGraph: ALWAYS call invalidateProvenance(priorGraph.triples, pathsToDrop) even if changedPaths is empty and sources_skipped_fresh &gt; 0 or only removals occurred
       - Also handle: priorGraph exists, all discovered files fresh, but some manifest entries deleted → still invalidate + re-normalize + publish
       - Fix early return / skip that currently only strips when sources_skipped_fresh &gt; 0 with non-empty changed set (RESEARCH Pitfall 1)
       - When full:true, do not reuse prior triples (existing full rebuild behavior)

    3. tests/maintain.test.ts: unit M1 first; integration deleted file with temp dirs + build (mirror build-pipeline.test.ts temp store pattern). Export invalidateProvenance and maintain from index.ts.
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern='maintain|invalidate|M1|deleted'</automated>
  </verify>
  <done>M1 green; deleted-source incremental build drops orphan provenance; invalidateProvenance is the single pure helper (MNT-01, D-05, D-06).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete M2–M5 matrix + maintain alias contract</name>
  <files>src/pipeline/maintain.ts, tests/maintain.test.ts</files>
  <behavior>
    - M2: drop both provenance sources → triple gone
    - M3: single EXTRACTED entry; drop that path → triple gone
    - M4: two EXTRACTED different paths; drop one → remains EXTRACTED
    - M5: multiset merge mixed tiers → confidence EXTRACTED if any remaining entry is EXTRACTED (via bestTier; may assert via normalize union then invalidate, or direct invalidate after mixed entries)
    - maintain({ corpus, dir }) equals build({ corpus, dir, full: false }) for sources_skipped_fresh / counts on a second-run fixture (alias only)
  </behavior>
  <action>
    Finish DESIGN M1–M5 unit matrix and alias smoke (D-05, D-06, D-12, RESEARCH OQ-1).

    1. Unit tests construct Triple objects with controlled provenance arrays (no FS) for M2–M4.

    2. M5: either feed normalize two candidate triples same s/p/o different tiers then assert bestTier, or construct multiset provenance and assert confidence after invalidate leaves EXTRACTED entry — document which path; both must use bestTier from ids.ts (D-02 shared ranks already exist).

    3. Alias test: after initial build, call maintain vs build full:false on same corpus; assert same sources_extracted / sources_skipped_fresh and loadGraphV1 triple counts match.

    4. Do not implement a second maintain orchestrator that re-extracts independently of build.
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern='maintain|M[1-5]|invalidate'</automated>
  </verify>
  <done>M1–M5 all pass; maintain is documented alias of build({ full: false }) (MNT-01, D-05, D-12).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: projectGraph, writeProjection wire, last-diff-base after build</name>
  <files>src/pipeline/project.ts, src/pipeline/build.ts, src/types.ts, src/index.ts, tests/maintain.test.ts, tests/build-pipeline.test.ts</files>
  <behavior>
    - projectGraph(v1) returns { nodes, edges } where each edge is { source: s, target: o, relation: p, label: p, confidence, id } from triples only — no invented triples (REP-01 prep, D-09)
    - build({ writeProjection: true }) writes graph.json after graph.v1 via publishGraphFiles (Pitfall 7 fix)
    - After successful build, store contains snapshots/.last-diff-base.json that validates as graph.v1 and matches published node/triple counts (OQ-3, DIFF-01 prep)
    - last-diff-base write occurs while build still holds the lock (D-10)
  </behavior>
  <action>
    Add projection helper and lifecycle baseline write on the success path (D-09, D-10, D-11, D-12; RESEARCH OQ-3).

    1. src/pipeline/project.ts — projectGraph(v1: GraphV1Document) mapping DESIGN edge projection fields. Pure function; no FS.

    2. build.ts publishGraphFiles call: when writeProjection true (opts or default), pass projection: projectGraph(graphV1). Keep DEFAULT_WRITE_PROJECTION false product default.

    3. After successful publishGraphFiles (still inside runBuild under lock):
       - ensure snapshots/ under store via confineUnderRoot / mkdir
       - write full graphV1 JSON atomically to snapshots/.last-diff-base.json (use writeJsonAtomicTemp + rename or equivalent safe-json helpers)
       - path must be confined under store root (STORE-05)

    4. Tests: writeProjection true creates graph.json with edge_count matching triple_count; last-diff-base exists and parseable; default writeProjection false still omits graph.json but still writes last-diff-base.

    5. Export projectGraph, invalidateProvenance, maintain from src/index.ts. Copyright headers (D-11).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>build incremental MNT-01 complete; projection payload + last-diff-base land under lock (D-06, D-09, D-10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus FS → build incremental | File add/change/delete drives provenance mutation |
| build → store writers | graph.v1, projection, last-diff-base under lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | incremental invalidation skip | high | mitigate | Always pathsToDrop = changed ∪ removed when !full and priorGraph (D-06) |
| T-03-05 | Tampering | concurrent maintain/build | high | mitigate | Existing acquireBuildLock on build (D-10) |
| T-03-06 | Tampering | last-diff-base path escape | high | mitigate | confineUnderRoot under store/snapshots |
| T-03-SC | Tampering | npm installs | high | mitigate | No new packages |
</threat_model>

<verification>
- npm test including M1–M5 and deleted-source cases
- build writeProjection true → graph.json edges from triples only
- snapshots/.last-diff-base.json after successful build
</verification>

<success_criteria>
MNT-01 true: multiset invalidation M1–M5 + deleted sources on build({ full: false }); maintain alias; baseline + projection wiring ready for SNAP/DIFF/REP.
</success_criteria>

<output>
Create `.planning/phases/03-query-lifecycle-maintain/03-02-SUMMARY.md` when done
</output>
