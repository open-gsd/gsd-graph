---
phase: 03-query-lifecycle-maintain
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - src/pipeline/snapshot.ts
  - src/types.ts
  - src/index.ts
  - tests/snapshot.test.ts
autonomous: true
requirements:
  - SNAP-01
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "snapshotSave writes full graph.v1 under store snapshots/ as ISO-name.json and returns snapshot metadata (SNAP-01, D-07)"
    - "snapshotList returns named snapshots newest-first, excluding .last-diff-base.json (SNAP-01, OQ-3)"
    - "snapshotRestore replaces graph.v1 from a named snapshot via publish under lock without inventing triples (SNAP-01, D-07, D-10)"
    - "Invalid snapshot names (.., separators, empty) throw PATH_ESCAPE (D-07, STORE-05)"
  artifacts:
    - path: src/pipeline/snapshot.ts
      provides: "snapshotSave, snapshotList, snapshotRestore"
    - path: tests/snapshot.test.ts
      provides: "round-trip + PATH_ESCAPE gates"
  key_links:
    - from: snapshotSave/restore
      to: acquireBuildLock
      via: "all mutating snapshot ops hold .build.lock (D-10)"
    - from: snapshot paths
      to: confineUnderRoot
      via: "snapshots/ under store only (D-07, STORE-05)"
    - from: snapshotRestore
      to: publishGraphFiles + loadGraphV1 validate
      via: "Ajv-valid graph.v1 only; no invented triples"
---

<objective>
Ship snapshot save/list/restore for full graph.v1 under store snapshots/ with lock, name sanitization, and round-trip integrity.

Purpose: SNAP-01 lifecycle rollback surface; baselines also feed DIFF-01 named snapshot compares.
Output: snapshot.ts, Snapshot types, tests/snapshot.test.ts, public exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-query-lifecycle-maintain/CONTEXT.md
@.planning/phases/03-query-lifecycle-maintain/RESEARCH.md
@docs/DESIGN.md
@src/pipeline/build.ts
@src/io/atomic-publish.ts
@src/io/load-graph.ts
@src/io/lock.ts
@src/io/paths.ts
@src/io/safe-json.ts
@src/errors.ts
@src/types.ts
@src/index.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end snapshot save → list → restore graph.v1</name>
  <files>src/pipeline/snapshot.ts, src/types.ts, src/index.ts, tests/snapshot.test.ts</files>
  <behavior>
    - After build into temp store, snapshotSave({ dir, name: 'pre-edit' }) creates snapshots/&lt;iso&gt;-pre-edit.json containing full graph.v1 document (SNAP-01, D-07, OQ-3)
    - snapshotList({ dir }) includes the saved snapshot (name or fileName), excludes .last-diff-base.json, ordered newest first (OQ-3, A5 no auto-prune)
    - Mutate store graph (second build with different corpus or direct publish of altered v1), then snapshotRestore({ dir, name: 'pre-edit' }) restores prior node/triple ids via loadGraphV1 (SNAP-01, D-07)
    - restore and save hold acquireBuildLock and release it (D-10)
  </behavior>
  <action>
    Implement snapshot lifecycle vertical slice (per D-07, D-10, D-11, D-12; RESEARCH OQ-3 RESOLVED).

    1. Types in src/types.ts:
       - SnapshotSaveOptions / SnapshotRestoreOptions: { dir?: string; name: string }
       - SnapshotListOptions: { dir?: string }
       - SnapshotResult: { name: string; fileName: string; path: string } (discretion fields OK)
       - SnapshotInfo: { name: string; fileName: string; path: string; mtime_ms?: number; built_at?: string }

    2. src/pipeline/snapshot.ts:
       - SNAP_DIR = 'snapshots'; LAST_DIFF_BASE basename stays '.last-diff-base.json' (written by build; list must skip it)
       - sanitizeSnapshotName(name): reject empty, '..', '/', '\\'; allow only /^[A-Za-z0-9._-]+$/; else GraphError PATH_ESCAPE
       - snapshotSave: lock → loadGraphV1 → iso timestamp with colons→hyphens → fileName `${iso}-${safe}.json` → confineUnderRoot(store, `snapshots/${fileName}`) → atomic write full graph JSON → release
       - snapshotList: list snapshots/*.json excluding .last-diff-base.json; sort mtime desc; no auto-prune (A5)
       - snapshotRestore: lock → resolve snapshot file by exact name match against sanitized name (match suffix -name.json or stored metadata) → read + validateGraphV1 → publishGraphFiles({ graphV1, writeProjection: false or true with projectGraph if desired — restore replaces v1; optional projection rewrite via projectGraph is OK if it invents no triples }) → release
       - Name resolution discretion: accept either the logical name ('pre-edit') matching *-&lt;name&gt;.json newest if multiple, or full fileName — document choice in module header; tests use logical name

    3. tests/snapshot.test.ts with temp store + multi-hop or structured-edges corpus build, save/list/restore round-trip asserting triple ids equal pre-mutation set.

    4. Export snapshotSave, snapshotList, snapshotRestore + types from index.ts.
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern='snapshot'</automated>
  </verify>
  <done>save/list/restore round-trips graph.v1 under snapshots/ with lock (SNAP-01, D-07, D-10).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Snapshot name confinement and restore validation</name>
  <files>src/pipeline/snapshot.ts, tests/snapshot.test.ts</files>
  <behavior>
    - snapshotSave name '..', '../x', 'a/b', 'a\\b', '' each throw GraphError with reason PATH_ESCAPE (D-07)
    - snapshotRestore of missing name throws a clear error (SCHEMA_INVALID or dedicated message — not silent empty success)
    - Corrupt snapshot file failing Ajv does not publish and leaves prior graph.v1 intact when possible
    - Concurrent-style: after restore, lock is released (second acquireBuildLock succeeds)
  </behavior>
  <action>
    Harden snapshot security and failure modes (D-07, D-10, D-12, STORE-05, RESEARCH Pitfall 5).

    1. Ensure all snapshot file paths go through confineUnderRoot(storeRoot, relative).

    2. PATH_ESCAPE tests for traversal-like names.

    3. Restore validates with validateGraphV1 / load path equivalent before publishGraphFiles.

    4. Document restore scope in module header: replaces graph.v1 (and optionally projection); does not roll back sources.manifest by default (RESEARCH A2) — sidecars unchanged unless you explicitly choose to leave them.

    5. No new packages. Copyright headers (D-11).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>SNAP-01 complete with PATH_ESCAPE gates and failed-restore safety (D-07, D-10, D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller name → snapshots/ | Snapshot names must not escape store root |
| restore → graph.v1 publish | Untrusted snapshot file must validate before replace |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Tampering / Elevation | snapshot name traversal | high | mitigate | sanitizeSnapshotName + confineUnderRoot; PATH_ESCAPE (D-07) |
| T-03-08 | Tampering | concurrent restore/build | high | mitigate | acquireBuildLock on save/restore (D-10) |
| T-03-09 | Tampering | invalid snapshot publish | high | mitigate | validateGraphV1 before publishGraphFiles |
| T-03-SC | Tampering | npm installs | high | mitigate | No new packages |
</threat_model>

<verification>
- npm test snapshot suite: round-trip + PATH_ESCAPE
- snapshots/ layout matches OQ-3
</verification>

<success_criteria>
SNAP-01 true: save/list/restore full graph.v1 under snapshots/ with lock and confined names (D-07, D-10).
</success_criteria>

<output>
Create `.planning/phases/03-query-lifecycle-maintain/03-03-SUMMARY.md` when done
</output>
