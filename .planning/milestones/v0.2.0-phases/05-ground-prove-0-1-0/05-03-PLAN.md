---
phase: 05-ground-prove-0-1-0
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - src/cli.ts
  - tests/cli-commands.test.ts
  - tests/cli.test.ts
autonomous: true
requirements:
  - PACK-01
  - ANS-01
  - ANS-02
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CLI registers pack and answer as top-level commands with JSON stdout via writeOk (D-06, K22)"
    - "pack &lt;question&gt; [--budget N] and answer &lt;question&gt; [--budget N] honor global --dir (D-06, D-10)"
    - "Empty / abstain library results still exit 0 with JSON body (not exit 2) (D-04, ANS-02, pitfall 4)"
    - "Phase 4 tests that expected pack/answer exit 1 are rewritten to registered happy-path; true unknown verb still exit 1 (D-06, pitfall 5)"
  artifacts:
    - path: src/cli.ts
      provides: "pack + answer commander adapters; remove unregistered comment"
    - path: tests/cli-commands.test.ts
      provides: "pack/answer registration + JSON smoke"
    - path: tests/cli.test.ts
      provides: "exit matrix without treating pack/answer as unknown"
  key_links:
    - from: pack action
      to: packSubgraph
      via: "withDir({ question, budget? }, globalDir) (D-06)"
    - from: answer action
      to: answer
      via: "same thin adapter pattern as query (D-06)"
    - from: main catch
      to: mapCliError
      via: "unchanged GraphError → 2/3; missing arg → CommanderError → 1"
---

<objective>
Register CLI `pack` and `answer` as thin K22 adapters over library APIs and flip Phase 4 unregistered-command tests (D-06, D-04, D-10, D-12).

Purpose: Agents can ground via the same machine contract as query/path; CLI-01 pack/answer verbs become real.
Output: Updated `src/cli.ts` plus rewritten cli-commands and cli exit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ground-prove-0-1-0/CONTEXT.md
@.planning/phases/05-ground-prove-0-1-0/RESEARCH.md
@.planning/phases/05-ground-prove-0-1-0/05-01-SUMMARY.md
@.planning/phases/05-ground-prove-0-1-0/05-02-SUMMARY.md
@src/cli.ts
@src/pipeline/pack.ts
@src/pipeline/answer.ts
@tests/cli-commands.test.ts
@tests/cli.test.ts
@tests/fixtures/corpus/multi-hop.jsonl
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Register pack and answer CLI adapters</name>
  <files>src/cli.ts, tests/cli-commands.test.ts</files>
  <behavior>
    - main(['node','gsd-graph','pack', question, '--dir', store]) after init+build multi-hop exits 0; stdout JSON has seeds/triples/paths fields (D-06)
    - main(... 'answer', question, '--dir', store) exits 0; JSON has pack, answer_markdown, mode, abstained (D-06, ANS-01)
    - optional --budget parseInt forwarded to library (D-06)
    - missing question argument → exit 1 usage (CLI-02)
    - remove or replace the comment that pack/answer are intentionally unregistered (D-06)
  </behavior>
  <action>
    Wire pack/answer as thin commander actions mirroring query (per D-06, D-10, RESEARCH Pattern 3).

    1. Import packSubgraph and answer from ./pipeline/pack and ./pipeline/answer (or ./index — match existing cli import style for build/query).

    2. Register before return program:
       - pack: argument &lt;question&gt;; option --budget &lt;n&gt; with parseIntOpt; action calls packSubgraph(withDir({ question, budget? }, globalDir(cmd))); writeOk(result)
       - answer: same shape calling answer(...); writeOk(result)

    3. Do not throw on empty pack — library returns abstain/empty; writeOk still runs (D-04 exit 0).

    4. Replace Phase 4 comment at former unregistered site with a short note that pack/answer are Phase 5 grounding verbs.

    5. tests/cli-commands.test.ts: replace it('pack and answer are unregistered...') with happy-path tests using temp store (init + build from multi-hop.jsonl only), captureIO main, assert code 0 and JSON.parse(stdout). Cover at least one answer deterministic case.
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern='cli-commands|pack|answer'</automated>
  </verify>
  <done>CLI pack/answer registered and smoke-tested via main(argv) JSON exit 0 (D-06).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip process-level exit matrix for pack/answer</name>
  <files>tests/cli.test.ts, tests/cli-commands.test.ts</files>
  <behavior>
    - tests/cli.test.ts no longer expects pack/answer in the unknown-command exit-1 loop (D-06, pitfall 5)
    - true unknown verb (e.g. not-a-real-command) still exit 1 with usage JSON
    - optional: spawn or main pack/answer success after fixture build still exit 0 (D-06)
    - abstain path (free-prose store or unmatched question) answer exit 0 with abstained true if covered here or deferred to golden plan — if covered, assert code 0 not 2 (ANS-02)
  </behavior>
  <action>
    Update process/E2E CLI tests so registration does not break the suite (per D-06, pitfall 5, D-12).

    1. In tests/cli.test.ts it('unknown command and unregistered pack/answer exit 1'):
       - Rename to unknown command exit 1
       - Keep unknown verb cases only
       - Remove pack and answer from the exit-1 expectation list
       - Add a separate it for pack/answer registered success if not already covered in cli-commands (prefer one process-level or main-level success assert)

    2. Ensure full npm test still green for cli suites after flips.

    3. Do not implement goldens G0/G1 here (plan 05-04) beyond minimal CLI smoke.
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern='cli|pack|answer|unknown'</automated>
  </verify>
  <done>Phase 4 unregistered expectations removed; pack/answer exit 0 happy path; unknown still exit 1 (D-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| argv question/budget/dir → library | Untrusted CLI input; thin adapter only |
| CLI stdout | Must remain pure JSON on success (K22) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08 | Tampering | --dir | high | mitigate | Pass through withDir → resolveStoreRoot / loadGraphV1 only (D-10) |
| T-05-09 | Information disclosure | stdout JSON | low | accept | Local graph answers; no ambient secrets |
| T-05-10 | Denial of service | huge question | low | accept | Existing hop/budget clamps in library |
| T-05-SC | Tampering | npm installs | high | mitigate | No new packages |
</threat_model>

<verification>
- cli-commands and cli tests green with pack/answer registered
- unknown command still exit 1
</verification>

<success_criteria>
- D-06 complete: pack/answer on CLI K22 contract
- Phase 4 negative tests flipped without losing unknown-verb coverage
</success_criteria>

<output>
Create `.planning/phases/05-ground-prove-0-1-0/05-03-SUMMARY.md` when done
</output>
