---
phase: 05-ground-prove-0-1-0
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/types.ts
  - src/pipeline/answer.ts
  - src/index.ts
  - tests/pack-answer.test.ts
autonomous: true
requirements:
  - ANS-01
  - ANS-02
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "answer() on a non-empty multi-hop pack returns mode deterministic, abstained false, and markdown with Seeds / Relationships / Paths / Citations sections (ANS-01, D-03)"
    - "Every citation triple_id in the answer body and pack.citations is ⊆ pack.triples ids (ANS-01, D-03)"
    - "Empty pack yields mode abstain, abstained true, abstain_reason empty_subgraph, and no fabricated relationship lines (ANS-02, D-04)"
    - "No LLM / prompt_pending / http apply path is exercised in Phase 5 (D-05)"
    - "answer loads graph only via packSubgraph (opts.graph or loadGraphV1) (D-10)"
  artifacts:
    - path: src/pipeline/answer.ts
      provides: "deterministic answer() + abstain branch"
    - path: src/types.ts
      provides: "GroundedAnswer, AnswerOptions"
    - path: tests/pack-answer.test.ts
      provides: "ANS-01 / ANS-02 gates"
  key_links:
    - from: answer
      to: packSubgraph
      via: "single composition entry; AnswerOptions extends PackOptions (D-03, D-10)"
    - from: answer_markdown Relationships/Citations
      to: pack.triples
      via: "iterate triples only — never invent edges (ANS-01, ANS-02)"
    - from: abstain branch
      to: GSD_GRAPH_REASON.EMPTY_SUBGRAPH
      via: "abstain_reason string only; no throw (D-04)"
---

<objective>
Ship pure deterministic `answer()` over `packSubgraph`: cited markdown for non-empty packs and honest abstain for empty packs (ANS-01, ANS-02, D-03, D-04, D-05, D-10, D-11, D-12).

Purpose: Relationship answers with triple citations — without LLM — complete the Ground stage library surface.
Output: `src/pipeline/answer.ts`, GroundedAnswer types, index exports, extended pack-answer tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ground-prove-0-1-0/CONTEXT.md
@.planning/phases/05-ground-prove-0-1-0/RESEARCH.md
@.planning/phases/05-ground-prove-0-1-0/05-01-SUMMARY.md
@src/pipeline/pack.ts
@src/types.ts
@src/index.ts
@src/errors.ts
@tests/pack-answer.test.ts
@tests/fixtures/corpus/multi-hop.jsonl
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Deterministic answer markdown with citations ⊆ pack</name>
  <files>src/types.ts, src/pipeline/answer.ts, src/index.ts, tests/pack-answer.test.ts</files>
  <behavior>
    - answer({ graph, question }) on multi-hop causes chain returns abstained === false, mode === 'deterministic' (ANS-01, D-03)
    - answer_markdown includes section headings Seeds, Relationships, Paths, Citations (order as DESIGN) (D-03)
    - Relationships and Citations lines derive only from pack.triples; citation ids ⊆ pack.triples (ANS-01)
    - answer.pack is the SubgraphPack from packSubgraph for the same options (D-03)
    - mode type allows 'prompt_pending' | 'http' for later phases but Phase 5 never sets them (D-05)
  </behavior>
  <action>
    Implement deterministic answer formatter over packSubgraph (per D-03, D-05, D-10, D-11, D-12).

    1. Types in src/types.ts:
       - GroundedAnswer: pack: SubgraphPack; answer_markdown: string; mode: 'deterministic' | 'prompt_pending' | 'http' | 'abstain'; abstained: boolean; abstain_reason?: string; prompt_bundle?: object (unused Phase 5)
       - AnswerOptions extends PackOptions (no LLM flags this phase — D-05)

    2. src/pipeline/answer.ts (D-11 copyright header):
       - answer(opts: AnswerOptions): GroundedAnswer
       - const pack = packSubgraph(opts)
       - If pack.triples.length === 0: return abstain shape in Task 2 — for this task implement the non-empty branch fully and a minimal empty branch so the function always returns (or implement empty fully if cheap in same file; tests for abstain are Task 2).

    3. Non-empty markdown template (D-03 / RESEARCH Pattern 2):
       - ## Seeds — one bullet per pack.seeds (use node id; optionally label if node present)
       - ## Relationships — one bullet per pack.triples as `s —p→ o (`triple_id`)`
       - ## Paths — one bullet per pack.paths rendering node-predicate chain
       - ## Citations — one bullet per pack.citations with triple_id, s —p→ o, optional source_path
       - Never invent a predicate or node not present in pack.triples / pack.paths.

    4. Export answer from src/index.ts.

    5. Extend tests/pack-answer.test.ts: multi-hop question → deterministic mode; markdown matches /causes/; citations ⊆ triples; pack.paths reflected in Paths section.
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern='answer|deterministic|citation|pack-answer'</automated>
  </verify>
  <done>Deterministic answer renders cited markdown from pack only (ANS-01, D-03, D-05).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Empty pack abstain — no fabricated relationships</name>
  <files>src/pipeline/answer.ts, tests/pack-answer.test.ts</files>
  <behavior>
    - Empty pack: mode === 'abstain', abstained === true, abstain_reason === 'empty_subgraph' (value of GSD_GRAPH_REASON.EMPTY_SUBGRAPH) (ANS-02, D-04)
    - Does not throw GraphError for empty pack (ANS-02, pitfall 4)
    - answer_markdown is empty string OR a single non-relational abstain note; must not contain relationship arrows like —causes→ or fabricated multi-hop prose (ANS-02, D-04)
    - pack field still present (empty triples) for consumers (D-04)
  </behavior>
  <action>
    Lock abstain honesty (per D-04, ANS-02, D-05, D-12).

    1. When pack.triples.length === 0:
       - return { pack, answer_markdown: '' or brief abstain note without claiming edges, mode: 'abstain', abstained: true, abstain_reason: GSD_GRAPH_REASON.EMPTY_SUBGRAPH }
       - Prefer importing GSD_GRAPH_REASON.EMPTY_SUBGRAPH for the reason string rather than a divergent literal.

    2. Tests:
       - answer({ graph: emptyOrNoMatchGraph, question: 'why does drought cause food shortage?' }) or stopword-only / unmatched question after pack empty
       - assert.equal(ans.abstained, true); assert.equal(ans.mode, 'abstain')
       - assert that answer_markdown does not match a causes relationship pattern (e.g. no —causes→)
       - assert function returns (no throw)

    3. Explicitly no LLM provider, no --apply-prompt-result, no network (D-05).
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern='abstain|empty|answer|pack-answer'</automated>
  </verify>
  <done>Empty pack abstains successfully with no fabricated relationships (ANS-02, D-04, D-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pack → answer markdown | Formatter must not invent edges beyond pack triples |
| question → answer | Free text must not trigger LLM or external calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Spoofing | answer_markdown | high | mitigate | Relationships/Citations iterate pack.triples only; abstain has no edge claims (ANS-01/02) |
| T-05-06 | Tampering | future LLM modes | medium | mitigate | Phase 5 never sets prompt_pending/http; no apply flags (D-05) |
| T-05-07 | Denial of service | huge markdown | low | accept | Bounded by applyBudget upstream; local CLI only |
| T-05-SC | Tampering | npm installs | high | mitigate | No new packages |
</threat_model>

<verification>
- pack-answer tests cover deterministic multi-hop answer and empty abstain
- citations ⊆ pack.triples
</verification>

<success_criteria>
- ANS-01 and ANS-02 library behavior complete
- Ready for CLI wiring in 05-03
</success_criteria>

<output>
Create `.planning/phases/05-ground-prove-0-1-0/05-02-SUMMARY.md` when done
</output>
