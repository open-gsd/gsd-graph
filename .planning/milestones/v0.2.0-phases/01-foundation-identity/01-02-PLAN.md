---
phase: 01-foundation-identity
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - schemas/graph-v1.schema.json
  - schemas/ontology-pack.schema.json
  - ontology-packs/general/ontology.json
  - ontology-packs/general/README.md
  - src/schema/validators.ts
  - src/ontology/types.ts
  - src/ontology/load-pack.ts
  - src/ontology/policy.ts
  - src/types.ts
  - src/index.ts
  - tests/ontology-load.test.ts
  - tests/ontology-policy.test.ts
  - tests/schema-validate.test.ts
autonomous: true
requirements:
  - ONT-01
  - ONT-02
  - ONT-03
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "loadOntologyPack for general validates and exposes closed typeSet and predicateSet from the pack allowlists"
    - "unknown policy review never allows write of unknown type/predicate; coerce maps type→Concept and predicate→related_to; drop discards"
    - "packs declaring extends are rejected or ignored — replace-only, no merge (ONT-03)"
    - "validateGraphV1 and validateOntologyPack are Ajv compile-once functions against checked-in JSON Schema files"
  artifacts:
    - path: schemas/graph-v1.schema.json
      provides: "graph.v1 on-disk authority"
    - path: schemas/ontology-pack.schema.json
      provides: "ontology pack authority"
    - path: ontology-packs/general/ontology.json
      provides: "default general pack fixture"
    - path: src/schema/validators.ts
      provides: "Ajv compile-once validators"
    - path: src/ontology/load-pack.ts
      provides: "loadOntologyPack replace-only loader"
    - path: src/ontology/policy.ts
      provides: "applyUnknownPolicy matrix"
    - path: tests/ontology-load.test.ts
      provides: "ONT-01/ONT-03 coverage"
    - path: tests/ontology-policy.test.ts
      provides: "ONT-02 matrix coverage"
  key_links:
    - from: src/schema/validators.ts
      to: schemas/*.schema.json
      via: "Ajv compile at module load (D-09)"
    - from: src/ontology/load-pack.ts
      to: ontology-packs/general/ontology.json
      via: "default packId general resolution"
    - from: src/ontology/policy.ts
      to: LoadedOntology typeSet/predicateSet
      via: "closed allowlist membership checks"
    - from: src/index.ts
      to: loadOntologyPack / applyUnknownPolicy / validateGraphV1
      via: "public façade re-exports"
---

<objective>
Ship checked-in JSON Schemas, Ajv validators, the general ontology pack, replace-only pack loading, and the review|coerce|drop unknown policy matrix.

Purpose: Closed-world ontology + schema authority is the contract every later pipeline stage depends on.
Output: schemas/, ontology-packs/general/, src/schema + src/ontology modules, load/policy tests, public exports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-identity/CONTEXT.md
@.planning/phases/01-foundation-identity/RESEARCH.md
@.planning/phases/01-foundation-identity/01-01-SUMMARY.md
@docs/DESIGN.md
@src/errors.ts
@src/index.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end load general ontology pack via Ajv</name>
  <files>schemas/graph-v1.schema.json, schemas/ontology-pack.schema.json, ontology-packs/general/ontology.json, ontology-packs/general/README.md, src/schema/validators.ts, src/ontology/types.ts, src/ontology/load-pack.ts, src/types.ts, src/index.ts, tests/ontology-load.test.ts, tests/schema-validate.test.ts</files>
  <behavior>
    - validateOntologyPack accepts ontology-packs/general/ontology.json
    - validateGraphV1 accepts a minimal DESIGN-shaped graph document and rejects missing required fields
    - loadOntologyPack() with default or packIdOrPath general returns LoadedOntology with pack.id general
    - typeSet contains Entity, Person, Concept, Document, Community (and full DESIGN node_types list)
    - predicateSet contains related_to, mentions, same_as (and full DESIGN predicate ids)
    - packHash is stable sha256 hex of canonical pack JSON bytes
    - invalid pack JSON or schema failure throws GraphError with ONTOLOGY_INVALID
  </behavior>
  <action>
    Implement schema-as-authority + general pack load path per D-05, D-09, ONT-01.

    1. schemas/ontology-pack.schema.json — draft-2020-12 ($schema field set). Require id, version, title, node_types, predicates; predicates items require id, domain, range arrays; unknown_predicate_policy and unknown_type_policy enum review|coerce|drop; strict boolean; additionalProperties policy: allow documented optional fields but do not invent extends-merge semantics. Prefer additionalProperties false on root unless RESEARCH/DESIGN needs flexibility for title-only extras.

    2. schemas/graph-v1.schema.json — draft-2020-12 matching RESEARCH skeleton: required schema_version const 1, engine const gsd-graph, engine_version, ontology_pack_id, ontology_version, built_at (date-time), nodes, triples; node items id/type/label; triple items id/s/p/o/confidence/provenance with confidence enum EXTRACTED|INFERRED|AMBIGUOUS; provenance minItems 1. Align with docs/DESIGN.md canonical model.

    3. ontology-packs/general/ontology.json — verbatim DESIGN general pack: node_types Entity, Person, Organization, Place, Concept, Document, Event, Claim, Topic, Community; full predicate list from DESIGN (related_to, mentions, part_of, derived_from, causes, supports, contradicts, located_in, works_for, authored, about, member_of, precedes, same_as) with domain/range arrays; strict true; both unknown_*_policy review (D-05).

    4. ontology-packs/general/README.md — short description: default replace-only general knowledge pack; how to copy and customize; no extends in v0.1.

    5. src/schema/validators.ts (D-08 header): use Ajv 2020 constructor (ajv/dist/2020) + ajv-formats; allErrors true; compile graph-v1 and ontology-pack schemas once at module load (D-09). Export validateGraphV1 and validateOntologyPack. Resolve schema JSON paths relative to package root (path.join from __dirname into ../../schemas when running from dist) so published package finds schemas/. Export helper formatAjvErrors if useful.

    6. src/ontology/types.ts: UnknownPolicy, OntologyPredicate, OntologyPack, LoadedOntology { pack, typeSet, predicateSet, packHash } per RESEARCH API sketch.

    7. src/ontology/load-pack.ts: loadOntologyPack({ packIdOrPath?, baseDir? }).
       - Default packIdOrPath general resolves to package ontology-packs/general/ontology.json (from package root / install root), not process.cwd store.
       - If packIdOrPath is an absolute or relative filesystem path to a .json file, read that path.
       - Read UTF-8 JSON, run validateOntologyPack; on failure throw GraphError(ONTOLOGY_INVALID).
       - Build ReadonlySet typeSet from node_types and predicateSet from predicates[].id.
       - packHash = sha256 hex of stable JSON serialization of the pack object (use sorted-keys stringify or the raw file bytes after validation — pick one, document in code comment, keep tests stable).
       - Replace-only: if parsed object has own property extends, throw GraphError(ONTOLOGY_INVALID) with message that pack composition is not supported in v0.1 (ONT-03 / D-05). Do not merge packs.
       - Return LoadedOntology; freeze pack object if practical.

    8. Expand src/types.ts with GraphNode, Triple, ProvenanceEntry, GraphV1Document interfaces mirroring schema (types mirror schema; schema remains authority — D-09).

    9. Re-export from src/index.ts: loadOntologyPack, validateGraphV1, validateOntologyPack, ontology types.

    10. tests/ontology-load.test.ts and tests/schema-validate.test.ts (node:test, D-10): happy-path general load; allowlist membership; invalid pack; minimal graph validate accept/reject.

    All new source files get D-08 copyright headers.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>general pack loads with closed allowlists; Ajv validators compile once; ONT-01 satisfied (D-05, D-09).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unknown type/predicate policy matrix (ONT-02)</name>
  <files>src/ontology/policy.ts, src/index.ts, tests/ontology-policy.test.ts</files>
  <behavior>
    - Known type/predicate → action allow
    - unknown + policy review → action review (caller must not write candidate)
    - unknown type + coerce → action coerce, coercedTo Concept
    - unknown predicate + coerce → action coerce, coercedTo related_to
    - unknown + drop → action drop
    - Default pack policies are review when loaded from general
  </behavior>
  <action>
    Implement applyUnknownPolicy pure helper per D-05 / ONT-02 and DESIGN policy matrix (docs/DESIGN.md unknown type/predicate table).

    Signature: applyUnknownPolicy(loaded, kind: 'type' | 'predicate', proposed: string) → { action: 'allow' | 'review' | 'coerce' | 'drop'; coercedTo?: string }

    Rules:
    - If proposed is in the corresponding allowlist Set → allow
    - Else read loaded.pack.unknown_type_policy or unknown_predicate_policy (kind-selected)
    - review → { action: 'review' } — never expands lock; never implies graph write
    - coerce → { action: 'coerce', coercedTo: kind==='type' ? 'Concept' : 'related_to' }
    - drop → { action: 'drop' }
    - If policy field missing, treat as review when pack.strict is true (DESIGN: strict defaults both to review)

    Export from src/index.ts. Unit-test full matrix in tests/ontology-policy.test.ts without filesystem beyond loading general once.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Policy matrix review|coerce|drop implemented and tested; default review writes nothing (ONT-02, D-05).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace-only pack composition gate (ONT-03)</name>
  <files>src/ontology/load-pack.ts, tests/ontology-load.test.ts</files>
  <behavior>
    - Pack JSON with extends string or array fails load with ONTOLOGY_INVALID
    - Only one active pack is returned — no multi-pack merge API exists
    - Custom path pack without extends still loads when schema-valid
  </behavior>
  <action>
    Harden ONT-03 / D-05 replace-only semantics already sketched in load-pack:

    1. Fixture under tests/fixtures/ontology/pack-with-extends.json (minimal otherwise-valid pack plus extends: "general") — assert loadOntologyPack({ packIdOrPath: fixturePath }) throws GraphError with reason ontology_invalid.

    2. Fixture pack-custom.json without extends — loads successfully from path.

    3. Confirm public API has no mergeOntologyPacks / extends resolver. Do not implement pack inheritance (v2 ONT-05 is out of scope).

    4. Document in ontology-packs/general/README.md one sentence: v0.1 is replace-only; copy the pack and point config/path at the copy.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>extends rejected; replace-only load only (ONT-03, D-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pack file path → loader | Untrusted JSON ontology files enter validation |
| schema files → Ajv | Checked-in schemas define accept/reject for store documents |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | ontology pack JSON | high | mitigate | Ajv fail-closed ONTOLOGY_INVALID; closed allowlists; no silent lock expansion (D-05, D-09) |
| T-01-05 | Tampering | graph.v1 candidate docs | high | mitigate | validateGraphV1 fail-closed SCHEMA_INVALID for later publish path |
| T-01-06 | Elevation of privilege | pack extends / open-world | medium | mitigate | reject extends; policy default review does not write unknowns (ONT-02/03) |
| T-01-07 | Denial of service | hostile huge pack JSON | low | accept | Phase 1 unit-test scale; hard size caps arrive with build pipeline |
| T-01-SC | Tampering | npm installs | high | mitigate | No new packages beyond plan 01-01 audited set |
</threat_model>

<verification>
- npm test covers ontology-load, ontology-policy, schema-validate
- loadOntologyPack('general') returns full DESIGN allowlists
- Policy matrix unit tests green for review/coerce/drop
- extends fixture fails closed
</verification>

<success_criteria>
ONT-01, ONT-02, ONT-03 true: general pack + policy matrix + replace-only semantics.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-identity/01-02-SUMMARY.md` when done
</output>
