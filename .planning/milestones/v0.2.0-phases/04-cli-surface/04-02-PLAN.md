---
phase: 04-cli-surface
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/cli.ts
  - tests/cli-commands.test.ts
autonomous: true
requirements:
  - CLI-01
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CLI registers init, build, query, path, status, diff, snapshot, review, repair, ontology (CLI-01, D-02)"
    - "pack and answer are not registered; unknown command exits 1 (D-02 Phase 5 deferral)"
    - "Each registered command is a thin adapter over existing library exports — no pipeline reimplementation (D-06)"
    - "Nested verbs: snapshot save|list|restore; review list|accept|reject; ontology show|validate (discretion + DESIGN)"
    - "build is offline-only: no --llm flag (D-08); --corpus required; optional --full and global --dir (D-07)"
    - "query/path map to library query IR; path uses query({ path: { from, to, maxDepth } }) (D-06)"
    - "review accept/reject resolve storeRoot via resolveStoreRoot before reviewResolve (D-06)"
  artifacts:
    - path: src/cli.ts
      provides: "Full Phase 4 commander surface over library ops"
    - path: tests/cli-commands.test.ts
      provides: "Per-command registration + thin mapping smoke via main(argv)"
  key_links:
    - from: build action
      to: build()
      via: "{ corpus, full?, dir? } from flags (D-06, D-07, D-08)"
    - from: path action
      to: query()
      via: "path: { from, to, maxDepth: depth }"
    - from: review accept/reject
      to: reviewResolve()
      via: "storeRoot = resolveStoreRoot({ dir }) first"
    - from: snapshot/review/ontology parents
      to: nested commander commands
      via: ".command('snapshot') without executable-string form"
---

<objective>
Wire the full Phase 4 command surface as a thin commander adapter over existing library exports (CLI-01, D-02, D-06).

Purpose: Agents can drive build/query/lifecycle/review/ontology through the same K22 contract established in 04-01; pack/answer stay Phase 5.
Output: Expanded `src/cli.ts` + `tests/cli-commands.test.ts` smoke coverage for each command.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-cli-surface/CONTEXT.md
@.planning/phases/04-cli-surface/RESEARCH.md
@.planning/phases/04-cli-surface/04-01-SUMMARY.md
@src/cli.ts
@src/index.ts
@src/types.ts
@src/pipeline/review.ts
@src/pipeline/query.ts
@src/ontology/load-pack.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire build, query, path, status, diff, repair</name>
  <files>src/cli.ts, tests/cli-commands.test.ts</files>
  <behavior>
    - build --corpus &lt;path&gt; [--full] uses global --dir; calls build({ corpus, full?, dir? }); writeOk(result); no --llm option registered (CLI-01, D-02, D-07, D-08)
    - query &lt;term&gt; [--hops N] [--budget N] → query({ term, hops?, budget?, dir? }); writeOk (CLI-01, D-06)
    - path &lt;from&gt; &lt;to&gt; [--depth N] → query({ path: { from, to, maxDepth: depth }, dir? }); writeOk (CLI-01, D-06)
    - status → status({ dir? }); writeOk (CLI-01)
    - diff [--snapshot &lt;name&gt;] → diff({ dir?, snapshot? }); writeOk (CLI-01)
    - repair → repair({ dir? }); writeOk (CLI-01)
    - GraphError from any of these still maps through existing main catch (exit 2/3) (CLI-02, D-03)
  </behavior>
  <action>
    Expand src/cli.ts with core ops as thin adapters (per D-02, D-06, D-07, D-08, D-09, D-10). Keep init from 04-01.

    1. Import build, query, status, diff, repair (and types as needed) from ./index.

    2. Register commands (commander nested/flat as top-level verbs):
       - build: required --corpus &lt;path&gt;; optional --full boolean; read global --dir; call build({ corpus: opts.corpus, full: opts.full, dir: global.dir })
       - query: argument &lt;term&gt;; --hops and --budget parseInt base 10; query({ term, hops, budget, dir })
       - path: arguments &lt;from&gt; &lt;to&gt;; --depth parseInt; query({ path: { from, to, maxDepth: depth }, dir })
       - status: no args; status({ dir })
       - diff: optional --snapshot &lt;name&gt;; diff({ dir, snapshot })
       - repair: repair({ dir })

    3. Every success path: writeOk(libraryResult) only — no console.log on stdout.

    4. tests/cli-commands.test.ts: for each command above, either (a) invoke main against a temp store prepared via library build from tests/fixtures/corpus (multi-hop.jsonl or structured-edges.md) after init, or (b) assert command is registered via program help text / main(['node','gsd-graph','help','build']) exit 0. Prefer real main invocations that return 0 with JSON.parse(stdout) for at least build+status+query on fixture corpus when store is prepared. Use NO_COLOR=1 style env if spawning; main() direct is fine for this plan.

    5. Do not register pack or answer (D-02).
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern='cli-commands|build|query|path|status|diff|repair'</automated>
  </verify>
  <done>Core ops commands emit JSON via library adapters (CLI-01, D-02, D-06, D-08).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire nested snapshot, review, ontology; leave pack/answer unregistered</name>
  <files>src/cli.ts, tests/cli-commands.test.ts</files>
  <behavior>
    - snapshot save &lt;name&gt; → snapshotSave({ name, dir? }); snapshot list → snapshotList({ dir? }); snapshot restore &lt;name&gt; → snapshotRestore({ name, dir? }) (CLI-01, D-02, D-06)
    - review list → loadReviewQueue(resolveStoreRoot({ dir })) or thin reviewList returning items/document; review accept &lt;id&gt; [--extend-ontology] → reviewResolve({ storeRoot, id, action:'accept', extendOntology? }); review reject &lt;id&gt; → reviewResolve reject (CLI-01, D-06)
    - ontology show|validate [--pack &lt;idOrPath&gt;] → loadOntologyPack({ packIdOrPath }); writeOk serializable subset (pack id, version, predicate/type counts or full pack fields that JSON.stringify cleanly) (CLI-01, D-06)
    - Nested parents use .command('snapshot') / .command('review') / .command('ontology') without the two-arg executable form that spawns external bins (RESEARCH pitfall 3)
    - main(['node','gsd-graph','pack']) and main(...'answer') return exit 1 (unknown) (D-02)
  </behavior>
  <action>
    Complete CLI-01 Phase 4 surface with nested commander verbs (discretion: nested per RESEARCH/DESIGN).

    1. Import snapshotSave, snapshotList, snapshotRestore, reviewResolve, loadReviewQueue, resolveStoreRoot, loadOntologyPack from ./index.

    2. Nested snapshot:
       - const snapshot = program.command('snapshot');
       - snapshot.command('save').argument('&lt;name&gt;').action → snapshotSave
       - snapshot.command('list').action → snapshotList
       - snapshot.command('restore').argument('&lt;name&gt;').action → snapshotRestore

    3. Nested review:
       - review list: resolve storeRoot; loadReviewQueue(storeRoot); writeOk queue document or { items: pending-filtered } — prefer pending filter for UX (RESEARCH A3 discretion: filter status==='pending' for list, include decisions count if useful)
       - review accept &lt;id&gt; with --extend-ontology boolean; reviewResolve({ storeRoot, id, action: 'accept', extendOntology }); writeOk({ ok: true, id, action: 'accept' }) or library return if any
       - review reject &lt;id&gt; similarly
       - CRITICAL: reviewResolve expects storeRoot absolute — always resolveStoreRoot({ dir: global.dir }) first (RESEARCH pitfall 4)

    4. Nested ontology:
       - ontology show [--pack &lt;idOrPath&gt;]: loadOntologyPack; writeOk JSON-safe summary (id, version, node_types length, predicates length, packHash if present)
       - ontology validate [--pack]: same load; on success writeOk({ ok: true, pack_id, version }); failures throw GraphError already from loader → exit 2

    5. Explicitly do not register pack or answer (D-02 Phase 5). Unknown command → commander usage → exit 1.

    6. Extend tests/cli-commands.test.ts:
       - snapshot save/list after a built store
       - review list returns JSON (empty queue OK)
       - ontology show|validate default general exit 0
       - pack and answer argv → exit 1
  </action>
  <verify>
    <automated>npm test -- --test-name-pattern='cli-commands|snapshot|review|ontology|pack|answer'</automated>
  </verify>
  <done>Full Phase 4 command list registered; pack/answer absent (CLI-01, D-02, D-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| argv flags/args → library options | Untrusted corpus paths, snapshot names, review ids, pack paths |
| CLI → store mutations | build/review/snapshot/repair write under store root |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering | snapshot name / corpus path | high | mitigate | Pass through library sanitizeSnapshotName / discover confinement; no shell exec of user strings |
| T-04-05 | Elevation of privilege | review accept --extend-ontology | medium | mitigate | Only reviewResolve with explicit flag; never ambient ontology expand (existing D-08 library) |
| T-04-06 | Information disclosure | ontology show | low | accept | Local pack metadata only; no secrets by design |
| T-04-07 | Denial of service | concurrent build via CLI | medium | mitigate | Existing .build.lock; GraphError build_locked → exit 3 from 04-01 mapper |
| T-04-SC | Tampering | npm deps | high | mitigate | No new packages this plan — commander/picocolors already pinned in 04-01 |
</threat_model>

<verification>
- `npm test` green with cli-commands coverage for all Phase 4 verbs
- `gsd-graph pack` / `answer` exit 1
- Nested snapshot/review/ontology help works without spawning external binaries
</verification>

<success_criteria>
- CLI-01 Phase 4 command set fully wired as thin library adapters
- pack/answer deferred unregistered
- review uses storeRoot correctly
</success_criteria>

<output>
Create `.planning/phases/04-cli-surface/04-02-SUMMARY.md` when done
</output>
